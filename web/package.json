{
  "name": "@boundless/admin-web",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Boundless admin web (SvelteKit). The SvelteKit app + UI is scaffolded in spec 001 task T15. Spec 001 T09 adds the framework-agnostic admin-WebAuthn edge verification module (web/src/lib/server/webauthn) + its tests, per docs/stack-matrix.md and ADR-0017.",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@simplewebauthn/server": "13.3.1"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "@types/node": "25.9.1",
    "typescript": "6.0.3",
    "vitest": "4.1.8"
  }
}
