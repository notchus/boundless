# 001 — Onboarding: Task List

> Tasks status: Draft (produced by `/speckit.tasks`, 2026-06-04)
> Spec: [`spec.md`](./spec.md) (Clarified) · Plan: [`plan.md`](./plan.md) (§10 resolved) · ADRs: 0001, 0014, 0015, 0016, 0017
> Contract: this file is what the `/speckit.implement` gate consumes. Anything not here is scope creep (P6). Every task maps to ≥1 spec AC (closes or enables).

> **Greenfield:** all trees (`core/`, `server/`, `api/`, `apple/`, `android/`, `web/`, `fixtures/`) are created by this slice. One task = one PR-sized slice. Each lists: **Does / Touches / Closes (AC) / Tests / Blocked by / ∥**.

## Dependency waves (parallelism map)

```
Wave 0:  T01 (scaffold)
Wave 1:  T02 (core domain types + fixtures)
Wave 2:  T03 (core::crypto)        ∥  T06 (migrations)
Wave 3:  T04 (auth codes + state)  ∥  T05 (session + refresh)
Wave 4:  T07 (member-auth endpoints) ∥ T08 (dev admin-create + invite + compat)
Wave 5:  T09 (admin WebAuthn — edge TS)
Wave 6:  T10 (API contracts + generated bindings)  ← CONTRACT-FREEZE GATE
Wave 7:  T11 ∥ T12 ∥ T13 ∥ T14 ∥ T15  (five UIs)
Wave 8:  T16 (cross-cutting verification)
```

**Hard serialization:** T01→T02→T03 and the T10 contract-freeze before any UI. **Most parallel:** Wave 7 (five UIs) and the within-wave pairs.

---

## Wave 0 — scaffolding

### T01 — Repo scaffolding, error-codes registry, CI drift gate — ✅ DONE (2026-06-04)
- **Does:** Create the workspace skeletons (`core/` crates, `server/`, `api/`, `fixtures/`); create `docs/error-codes.md` and seed the onboarding/auth error codes (P12); stand up the CI gate that (a) rejects hand edits to `api/generated/**` and (b) fails if `core/`/`api/` change without regenerated bindings (ADR-0001). Pin `dryoc 0.8.0` (corrected from 0.9.0 — see below) and `@simplewebauthn/server 13.x` in the manifests per `stack-matrix.md`.
- **Touches:** repo root, `core/*/Cargo.toml`, `server/`, `api/`, `fixtures/`, `docs/error-codes.md`, CI workflow, `rust-toolchain.toml` (dryoc needs Rust ≥1.89 — verify).
- **Closes/enables:** enables all; partially **AC13** (allow-list CI scaffold).
- **Tests:** CI dry-run green; binding-drift check fails on a deliberate ungenerated change (meta-test).
- **Blocked by:** —  · **∥:** no (blocks everything).
- **Done (2026-06-04):** `core/` workspace (8 skeleton crates) + `server/` + `api/` (placeholder `openapi.yaml`/`boundless.proto` + committed `generated/` tree) + `fixtures/` + `web/package.json`; `rust-toolchain.toml` → 1.95.0. `docs/error-codes.md` seeded (P12). Binding-drift gate = `scripts/{_bindings_common,generate-bindings,check-binding-drift,test-binding-drift}.sh` + `api/.bindings.lock`; network allow-list (AC13 scaffold) = `scripts/check-network-allowlist.sh` + `ci/{forbidden-trackers,network-allowlist}.txt`; wired in `.github/workflows/ci.yml`. **dryoc pin corrected `0.9.0` → `0.8.0`** (0.9.0 unpublished on crates.io) across `stack-matrix.md`/`plan.md`/this file/`DEFERRED.md`; `@simplewebauthn/server 13.3.1` pinned. All gates verified green locally (`cargo fmt/clippy/test --locked`, drift meta-test, allow-list across 3 lock files incl. `web/pnpm-lock.yaml`); `reviewer` findings H1/M2/M3/L1 fixed, M1 resolved (web lock now committed + scanned). CI itself runs on GitHub Actions (not triggerable locally).

---

## Wave 1 — core types

### T02 — Core domain & value types + golden fixtures — ✅ DONE (2026-06-04)
- **Does:** Define the P4-generated types in `core/domain`: `MemberId`, `Role`, `Platform`, `AppVersion`, `ClientVersion`, and the tainted newtypes `PhoneNumber`, `DeviceToken`, `OnboardingCode`, `RecoveryCode`, `AccessToken`/`RefreshToken` — all with **no `Debug`/`Display`**, only `redacted_summary()` (P2). Author the golden JSON fixtures in `fixtures/auth/**` and `fixtures/manifest/**` named in plan §5.
- **Touches:** `core/domain/`, `fixtures/`.
- **Closes/enables:** enables **AC4, AC7** (types); fixtures underpin AC8/AC10/AC15/AC19.
- **Tests:** `insta` serialization snapshots of each wire type + the `redacted_summary()` forms; a compile/inspection test asserting tainted types expose no `Debug`/`Display`.
- **Blocked by:** T01 · **∥:** no (blocks 3–6, 10).
- **Done (2026-06-04):** `core/domain` defines the value types (`MemberId` transparent-UUID; `Role`; `Platform` with canonical `ios/ipados/watchos/macos/android/wearos/web` wire names; `AppVersion` string wire-form with **semantic** (not lexicographic) ordering + `FromStr`/`Display`/custom serde; `ClientVersion{platform, app_version}`) and the six tainted newtypes (`PhoneNumber`/`DeviceToken`/`OnboardingCode`/`RecoveryCode`/`AccessToken`/`RefreshToken`) — **no `Debug`/`Display`/`Serialize`**, only `redacted_summary()` + `expose_secret()`, enforced at compile time via `static_assertions::assert_not_impl_any!` (P2/I3). 15 golden fixtures authored (`fixtures/auth/`×9, `manifest/`×4, `compat/`×2) + per-dir README descriptors; the manifest Ed25519 **signature vectors are deferred to T03** (content bytes frozen; documented in `fixtures/manifest/README.md`). Deps pinned from the lock into `docs/stack-matrix.md`: `serde` 1.0.228, `serde_json` 1.0.150, `uuid` 1.23.2 (**`serde` feature only — no `rng`/getrandom**, keeping the lib wasm-safe + randomness-free), `insta` 1.47.2, `static_assertions` 1.1.0. **14 tests** green; `cargo fmt`/`clippy -D warnings`/`test` all `--locked` clean; **`cargo build --target wasm32-unknown-unknown` clean** (lib dep graph = serde + uuid only); binding-drift gate green (27 inputs, lock regenerated); network allow-list clean (3 lock files, no trackers). Reviewed via a 4-agent adversarial workflow (`reviewer` + `security-auditor` + `platform-parity` + `test-strategist`): **0 confirmed in-scope defects** (security-auditor clean on P2/I3/I4/I8). Two low/info test-hardening notes applied (inbound JSON round-trip for `MemberId`/`ClientVersion`; explicit named-fixture presence check). `platform-parity`'s 3 notes are forward-guidance for the **T10** UniFFI codegen (AppVersion record-vs-string, MemberId custom-type mapping, tainted-type formatter-free binding surface) — out of scope here, noted for T10.

---

## Wave 2 — crypto & schema (parallel)

### T03 — `core::crypto` (dryoc + RustCrypto) — ✅ DONE (2026-06-04)
- **Does:** Phone **HMAC-SHA256 + constant-time compare** (I3); Onboarding/Recovery **code hashing at rest**; **Ed25519 detached-signature manifest verification** + the ADR-0014 **tiered fallback** (verify-fail→cached→bundled) and lower-`manifest_version`-ignore. All via `dryoc` (wasm32-safe). Injected RNG (no ambient randomness).
- **Touches:** `core/crypto/`, `core/crypto/tests/invariants.rs`.
- **Closes:** **AC3** (constant-time leg), **AC10** (verify + tiers — core leg).
- **Tests:** `i3_phone_lookup_constant_time`; `ac10_manifest_{verify_fail_with_cache,verify_fail_no_cache,lower_version_ignored,offline_first_launch}` (replay the `fixtures/manifest/**`).
- **Blocked by:** T02 · **∥:** with T06.
- **Done (2026-06-04):** `core/crypto` implements `hashing` (HMAC-SHA256 keyed `phone_lookup_hash` + Onboarding/Recovery `*_code_hash`, all with constant-time `verify_slice`, domain-separated; `HmacKey`/`PhoneLookupHash`/`CodeHash` carry **no `Debug`/`Display`/`Serialize`**, compile-time-proved) and `manifest` (`verify_manifest_signature` Ed25519 via dryoc against the **bundled** trusted key — never the envelope's; `canonical_manifest_bytes` sorted-key-compact signing contract; `decide_manifest` → `{ApplyFetched, KeepCached, UseBundled, IgnoreStale}` + `ManifestErrorCode` matching `error-codes.md`). **Crypto-library decision → ADR-0018:** dryoc 0.8.0 has **no HMAC-SHA256** (`crypto_auth` is HMAC-SHA512-256, `crypto_hash` is SHA-512 only), so the I3 keyed hash uses RustCrypto `hmac` 0.13.0 + `sha2` 0.11.0 — **I3 kept verbatim** (owner's call) — while dryoc stays the sole Ed25519 signature impl. **wasm32 fix:** dryoc transitively pulls `rand`→`getrandom 0.4` (un-feature-gated), which `compile_error!`s on `wasm32-unknown-unknown`; enabled `getrandom`'s `wasm_js` backend for wasm32 in `core/crypto/Cargo.toml` (compile-only — T03 uses zero randomness). `verify_ok.json` signature/public-key vector filled deterministically from a checked-in seed (regenerated + asserted by `verify_ok_vector_reproducible_from_seed`). **12 invariants tests** (incl. `manifest_verify_rejects_attacker_supplied_key`, `canonical_manifest_bytes_is_deterministic_sorted`, domain-separation, error-code-registry) + the 4 named `ac10_*` + `i3_phone_lookup_constant_time`. Gates green: `cargo fmt`/`clippy -D warnings`/`test --workspace` all `--locked`; **`cargo build --target wasm32-unknown-unknown` clean**; binding-drift lock regenerated; network allow-list clean. Stack-matrix corrected (dropped the false "dryoc=HMAC-SHA256" line; added `hmac`/`sha2`/`base64` rows); dryoc-bump check done (0.8.0 still latest). **Out-of-scope register → `DEFERRED.md` → Crypto** (I1 sealed-box→spec 008; code gen/TTL/rate-limit→T04/T07; phone normalization→T04; HmacKey provisioning + manifest-mint Worker + bundled pubkey→server/infra; zeroization + RNG-backend policy→hardening/T07).

### T06 — Database migrations `0001`–`0008` — ✅ DONE (2026-06-04)
- **Does:** Write numbered migrations: `groups`, `members` (`phone_lookup_hash`/`phone_encrypted`), `onboarding_codes`, `recovery_codes`, `device_tokens` (`(member_id,platform,app_version)`), `sessions` (rotation lineage), `admin_webauthn_credentials` (multi-per-admin), `admin_invitations`. Conventions: PII `*_encrypted bytea`, `created_at/updated_at/created_by`, RLS. **No `0009_admin_alerts`** (§10-E: counter lives in the DO). `pgcrypto` is **not** on the PII path (§10-H).
- **Touches:** `server/migrations/0001…0008_*.sql`.
- **Closes/enables:** enables **AC4, AC16, AC17, AC18, AC19, AC20**.
- **Tests:** migration up/down applies on a Postgres fixture; RLS smoke test on a PII table; schema assertion that PII columns are `bytea`.
- **Blocked by:** T02 (agreed schema) · **∥:** with T03.
- **Done (2026-06-04):** 8 **reversible** migrations (`server/migrations/NNNN_*.{up,down}.sql`, sqlx-`migrate!`-compatible naming verified via docs-researcher; **no in-file `BEGIN`/`COMMIT`** — the runner wraps each). Schema-only (no endpoint logic, no row writes, **no new Rust deps**). Conventions enforced on **every** table: `created_at`/`updated_at` (+ a shared `set_updated_at()` `BEFORE UPDATE` trigger) + `created_by` (audit, I5); PII/secret columns are **`bytea`** (`phone_lookup_hash`/`phone_encrypted` I3, `*_code_hash`, `token_encrypted` I4, `refresh_token_hash`, `token_hash`); **RLS `ENABLE`+`FORCE`** + a uniform `{table}_group_isolation` policy. Tenant isolation is centralized in one **fail-closed resolver `current_group_id()`** (in `0001`) that maps **both** an unset GUC and a reset-to-empty `''` (the pooled-Hyperdrive case) → NULL → deny — chosen over inline `current_setting(...)::uuid` after the live run showed `''::uuid` *errors* instead of denying. Member-scoped child tables carry a denormalized `group_id` + a **composite FK `(member_id, group_id) → members(id, group_id)`** so the denormalized tenant can't drift. Structural invariants via **partial unique indexes**: one live Onboarding/Recovery code per member (AC17 regenerate-invalidates-prior), one current credential per session family (AC18/T05 `RefreshPresentation`), one live invite per admin (AC16). `device_tokens` PK = `(member_id, platform, app_version)` (I4); `sessions` models the T05 rotation lineage (`family_id`/`parent_id`/`rotated_at`/`revoked_at`, indefinite-until-revoked). Enum types `member_role`/`device_platform` mirror `core::domain` wire forms. **Tests (two layers):** (A) dependency-free std-only **static convention test** `server/tests/migrations.rs` (7 tests — numbering+up/down pairing, audit cols, RLS predicate keyed on `current_group_id()` **with `WITH CHECK`** (not just policy-name presence), resolver-is-fail-closed, bytea PII, device_tokens PK, down reverts every table **+ type + function**, no `pgcrypto`/extension/in-file txn) — runs on pre-push + CI; (B) self-skipping **live psql script** `scripts/test-migrations.sh` (apply 0001→0008 → catalog asserts bytea + forced-RLS on all 8 → **RLS smoke: own-group visible / other-group denied / unset-GUC denied (fail-closed) / cross-group INSERT rejected by `WITH CHECK`** → revert 0008→0001 → clean teardown). **Verified for real against `postgres:16` (Docker):** full cycle green; cross-group write returns `ERROR: new row violates row-level security policy`, correct-tenant write succeeds. CI: `rust-core` server step `cargo build`→`cargo test --locked` (runs A); new **`server-migrations`** job with a `postgres:16` service runs B (CI is GitHub-only, not locally triggerable). Gates green: server `fmt`/`clippy -D warnings`/`test --locked`; core `test --workspace --locked` unchanged; binding-drift (42 inputs) + network allow-list (3 locks) unaffected. Reviewed via `reviewer` + `security-auditor`: **0 confirmed critical/high**; applied the worthwhile findings (strengthened the static RLS assertion to check the predicate + `WITH CHECK`; added deny-by-default + `WITH CHECK` legs to the live smoke; `DROP OWNED BY`+`trap` role teardown; `current_group_id()` fail-closed resolver; down reverts types/function; unified file ordering). **Out-of-scope register → `DEFERRED.md` → Server / migrations (T06)**: `SET LOCAL` GUC per request txn + non-superuser/non-BYPASSRLS DB role + atomic supersede-then-insert + `audit_log`/read-path I5 → **T07**; sqlx `Migrator` harness + `sqlx` pin (0.8.6) + stack-matrix fill → **T07**; PostGIS/`pgcrypto` + `address_encrypted` + per-Group key/KEK (I1) → **spec 008**; actual row writes → T07/T08.

---

## Wave 3 — core::auth (parallel)

### T04 — `core::auth`: state machine, version compare, Onboarding/Recovery code logic — ✅ DONE (2026-06-04)
- **Does:** Implement the device-side `OnboardingState` machine (incl. `NeedsReauthHelp`, `Offline` overlay, `ManifestFailReturning`, `BelowMinVersion`); `AppVersion` vs `client_min_version` comparison (O4/O1); Onboarding Code **single-use / TTL / rate-limit** request-shaping + result interpretation; Recovery Code lifecycle. Pure logic + **injected `Clock`** (no `SystemTime::now`).
- **Touches:** `core/auth/` (+ UniFFI surface), `core/auth/tests`.
- **Closes:** **AC17** (code single-use/TTL/rate-limit + regenerate-invalidates-prior — core leg); contributes **AC8/AC15** (state decision), **AC19** (recovery logic).
- **Tests:** `prop_onboarding_code_single_use_ttl_ratelimit` (proptest, seeds checked in); `ac17_regenerate_invalidates_prior`; version-compare/N-2 property test. `TestClock` for the wrong-client-clock case.
- **Blocked by:** T02, T03 · **∥:** with T05.
- **Done (2026-06-04):** `core/auth` now holds four pure modules (no `uniffi` attrs yet — codegen is T10, per T02/T03). `clock`: an injected `Clock` trait over a homegrown `UnixSeconds(i64)` UTC newtype + `FixedClock` (the `TestClock`); core never calls `SystemTime::now`. `version`: `evaluate_version` (O4 below-min gate, semantic `Ord` not lexicographic; robust to an inverted `min>recommended` requirement) + `minimum_supported` (O1 N-2 window, floored within the major). `code`: `evaluate_onboarding_code` (AC17 — gate order **consumed/superseded → rate-limit → expiry → constant-time match via `core::crypto`**, so `Accepted ⟺ live ∧ correct` with no timing oracle) and `evaluate_recovery_code` (AC19 — **driver-only role gate enforced in-core**, not delegated; `NotAvailable`/`Invalid`/`Accepted`; no TTL per ADR-0016 D3) + `recovery_available_for`. `state`: the full `OnboardingState` machine (`on_event` faithful to the spec table; `Offline`/`ManifestFailReturning` modeled as overlay/launch-outcome, **not** nodes; `BelowMinVersionDetected`/`SessionInvalidated` override from **any** state) + `reauth_state_for` (Rider→`NeedsReauthHelp`, Driver/Admin→`PhoneEntry`). All verdict/error codes map to existing `docs/error-codes.md` entries (no new codes). **40 tests:** the named `prop_onboarding_code_single_use_ttl_ratelimit` + `prop_n_minus_2_version_window` + `prop_recovery_code_single_use` (independent oracles), `ac17_regenerate_invalidates_prior`, the wrong-client-clock test, AC8/AC15 + happy-path **fixture replays** (`fixtures/auth/{signin_ok,below_min_version,needs_reauth_help}.json`), an **exhaustive transition-graph** sweep (defined edges + every undefined edge is a no-op + cross-cutting overrides from every state), boundary units (TTL-inclusive-at-now, rate-limit-at-max, `max_attempts==0`), `is_terminal` partition, and the P12 `auth_verdict_error_codes_match_registry`. **Crypto-library decision N/A** (composes T03's constant-time matchers). Deps: `boundless-crypto` (workspace path) + `serde`; **`proptest` 1.11.0** pinned (dev-only, latest published, verified via docs-researcher) — `docs/stack-matrix.md` filled. P9 seeds via proptest's idiomatic committed `proptest-regressions/` (on failure) + deterministic boundary units. Gates green: `cargo fmt`/`clippy -D warnings`/`test --workspace` all `--locked`; **`cargo build --target wasm32-unknown-unknown -p boundless-auth` clean**; binding-drift lock regenerated (38 inputs); network allow-list clean. Reviewed via a 3-lens adversarial pass (`reviewer` + `security-auditor` + `test-strategist`): **0 confirmed critical/high**; the one shared finding (recovery role gate was a caller-contract) was **fixed** by folding the gate into `evaluate_recovery_code(role)`; plus version-requirement robustness, the recovery property, exhaustive-graph + boundary tests, and a backtick-anchored registry check. **Out-of-scope register → `DEFERRED.md` → `core::auth` (T04)**: server-time enforcement + rate-limit window + Turnstile + **atomic consume-on-accept** + **sign-in response-timing parity** → T07; sessions/refresh-rotation/replay + device-token binding → T05; UniFFI export → T10; chrono-vs-time pick → T07; `Clock`/`UnixSeconds` promotion → as needed; **N-2-across-major support policy needs an ADR before `2.0.0`**.

### T05 — `core::auth`: sessions, refresh rotation, device-token binding — ✅ DONE (2026-06-04)
- **Does:** Indefinite session + **silent refresh-token rotation** with replay/lineage detection (ADR-0016 D2); device-token binding `(member_id, platform, app_version)` and **invalidation on new-device re-onboarding** (I4); the invalidation-trigger set (revoke/logout, re-onboard, delete). Secure at-rest storage contracts per §10-F (consumed by the UI tasks).
- **Touches:** `core/auth/`, `core/auth/tests`.
- **Closes:** **AC4** (re-onboarding invalidation — core leg), **AC18** (indefinite + silent refresh + triggers), contributes **AC19**.
- **Tests:** `i4_tokens_invalidated_on_reonboarding` (distinct from `…_on_logout`); `prop_session_indefinite_until_admin_event`; `ac18_invalidation_triggers_exactly[revoke,reonboard,delete]`; **`auth_refresh_rotation_replay_detected`** (the new privacy-invariant test — DEFERRED §G item 1, ships here).
- **Blocked by:** T02, T03 · **∥:** with T04.
- **Done (2026-06-04):** Two new pure modules in `core/auth` (no `uniffi` attrs yet — codegen is T10), composing only `boundless-domain` + the injected `Clock`; no crypto compare in this leg (the refresh hash + lineage lookup are server-side, T07). `session`: `SessionFamilyStatus{Active,Revoked}`; `Session{member_id, family_id, access_token_expires_at, family_status}` with **time-independent** `is_live` (the indefinite guarantee, AC18) and `needs_refresh(clock, skew)` (silent-refresh trigger); `RefreshPresentation{Current,Superseded,Unknown}` (server's lineage classification — input to the policy); `RefreshVerdict{Rotate,ReplayDetectedKillFamily,Rejected}` + `error_code()` + `resulting_family_status()` (replay ⇒ family `Revoked`); `evaluate_refresh` single-sources the rotation/replay policy. `device`: `DeviceBinding` (the I4 tuple); exhaustive admin-mediated `InvalidationTrigger{AdminRevokeOrLogout,NewDeviceReonboarding,AccountDeletion}` (no time-based variant, by construction); `TokenInvalidationScope{AllForMember,PriorDevice}` + `SessionInvalidation`; `invalidation_for` (revoke/logout & delete → AllForMember; re-onboard → PriorDevice, AC4) + `reonboarding_invalidation` (same-member only — cross-member isolation); `required_refresh_store(platform) → SecureStoreClass` (the §10-F contract as testable core) + `DEVICE_TOKEN_INVALIDATED_CODE`. Added **`SessionFamilyId`** to `core::domain` (transparent-UUID, parse/format only — mirrors `MemberId`). **No new error codes** (all map to existing `docs/error-codes.md`); **no new crate pins** (`uuid` added to `core/auth` *dev*-deps only — already a workspace dep). **55 auth tests** (incl. the 4 named: `auth_refresh_rotation_replay_detected` walking rotate→supersede→replay→family-revoked→legit-current-now-rejected→idempotent-second-replay; `prop_session_indefinite_until_admin_event` (independent refresh oracle over full `i64`); `ac18_invalidation_triggers_exactly`; `i4_tokens_invalidated_on_reonboarding` + `i4_tokens_invalidated_on_logout`; plus `prop_reonboarding_isolates_by_member` (I4 cross-member isolation), `required_refresh_store` mapping, the extended P12 registry check, and rotation/needs_refresh boundary+saturation units). Gates green: `cargo fmt`/`clippy -D warnings`/`test --workspace` all `--locked`; **`cargo build --target wasm32-unknown-unknown -p boundless-auth` clean**; binding-drift lock regenerated (42 inputs); network allow-list clean. The refresh-rotation control is recorded under **I4** in `docs/privacy-invariants.md` (it underwrites I4 — not a new numbered invariant, which the PII-scoped doc reserves). Reviewed via a 3-lens adversarial pass (`reviewer` + `security-auditor` + `test-strategist`): **0 confirmed critical/high**; applied `needs_refresh` skew → `u32` (illegal-negative-unrepresentable), single-sourced the `AUTH_SESSION_INVALIDATED` literal, and added the idempotent-replay leg + refresh property-oracle + i64-saturation unit + cross-member isolation property. **Out-of-scope register → `DEFERRED.md` → `core::auth` (T05)**: server refresh persistence/lineage-classification/at-rest-hashing + atomic replay-revoke + refresh rate-limit/shape-parity + multi-device-binding policy + access-token signing → T07/T06; APNs/FCM registration → spec 007/T07; UniFFI export → T10; `SecureStoreClass` wiring → T11–T15; I12 forgetting-extension + delete-leg token test stay deferred to `core::deletion`.

---

## Wave 4 — server (parallel)

### T07 — Server (workers-rs): member-auth endpoints + degradation + DO — 🟡 CORE LEG DONE (2026-06-04) + shell slice A DONE (2026-06-05) + async-port bridge DONE (2026-06-05) + shell slice 2 (access-token decision + port impls) DONE (2026-06-05) + **T07-shell-B slice 1 (toolchain bring-up + Worker skeleton) DONE (2026-06-07)**; remaining PG/deploy legs → **T07-shell-B** (`DEFERRED.md`)
- **Does:** `/api/auth/signin` (phone-hash lookup, no existence leak), `/api/auth/bind-device` (Onboarding Code, server-time validated, **cannot complete offline**), `/api/auth/refresh` (rotation), `/api/auth/recovery/rebind` (Driver). Every response carries `client_min_version` + `client_recommended_version` (O4/O5). `GroupHub` DO: rate-limit counter + token invalidation (I4); **below-min-version** path → calm screen + **one Queues admin alert/member/day** (non-PII); the **notification-declined** non-PII admin flag (AC14). All logging via `boundless::logging::emit()` (P2).
- **Touches:** `core/server/`, `server/`, DO class.
- **Closes:** **AC7** (server leg), **AC8**, **AC14** (server leg), **AC17/AC18/AC19** (enforcement legs), **AC4** (server leg); **I5** audit emission on any admin phone read path it exposes.
- **Tests:** `ac8_below_min_emits_one_alert_per_member_per_day` (Clock + Queue stub); `ac14_decline_records_nonpii_flag_and_advances`; `ac15_invalidated_rider_alert_once_per_day`; integration for bind/refresh/recovery; alert-payload-no-PII scrub.
- **Blocked by:** T03, T04, T05, T06 · **∥:** with T08.
- **Done — Layer A (core leg), 2026-06-04:** Built the **pure, deterministic, wasm-safe member-auth orchestration engine** in `core/server` (`boundless-server-core`) — the *functional core*, with the I/O boundary as **port traits** (`AuthStore`/`AdminAlertSink`/`SecretSource`) + the injected `Clock`/`SecretSource`; in-memory stubs in tests, Cloudflare/Postgres impls in the deferred Worker shell. `AuthService<St,Sk,Sec,Clk>` composition root with: `sign_in` (AC7-data + AC8 below-min-first + **no-existence-leak** uniform shape, carry-forward (b)); `bind_device` (AC17 server-time lifecycle via `evaluate_onboarding_code` + **atomic consume-on-accept**, carry-forward (a); AC4 device-token (re)bind invalidating **all** prior bindings — decision: single active device per member, F5; rate-limit window → lock + deduped alert; device-token-invalidated is **silent**, carry-forward (e)); `record_notification_decision` (AC14 non-PII flag + advance); `refresh` (AC18 rotate via `evaluate_refresh` / **replay → atomic family-kill + AC15 alert** / uniform `Invalidated` for unknown/revoked/replay — **no lineage leak**, carry-forward (a)/(e); `server_verdict` is server-only ops, never client-facing); `recovery_rebind` (AC19 Driver-only, rotated code, old token invalidated). Added: `GroupHubState` (§10-E — attempt window + per-member-per-day alert dedup + refresh-rejection counter); PII-free `AdminAlert` enum; `normalize_phone` E.164 (single-source for spec-008 issuance, T03/T04 carry-forward); and the one in-scope crypto primitive `core::crypto::refresh_token_hash`/`refresh_token_matches` (`RefreshTokenHash`, domain tag `boundless:refresh-token:v1`, constant-time; T05→T07 carry-forward) + `AUTH_NOTIFICATIONS_NOT_ENABLED` in `error-codes.md` (P12). **No new external deps** (composes `boundless-domain`/`auth`/`crypto` + `serde`; dev `proptest`/`uuid`/`serde_json`/`static_assertions`). **48 server-core tests** (signin 4, bind 18, refresh 7, recovery 6, hub/alerts 6, phone 3, error_codes 1, no_formatter 1, properties 2 — incl. **`auth_refresh_rotation_replay_detected`** server leg, **`ac8_…per_member_per_day`**, **`ac15_…once_per_day`**, **`reonboarding_with_multiple_prior_bindings_invalidates_all`**, `bind_atomic_consume_no_double_bind`, `ac17_regenerated_code_supersedes_prior`, `ac17_correct_code_after_lock_is_still_rate_limited`, TTL-boundary units, `admin_alert_payload_is_pii_free`, the compile-time `no_formatter` P2 guard on the secret-bearing responses, `prop_bind_never_double_consumes`, `prop_alert_at_most_once_per_member_per_day`) + **3 new crypto tests**. Gates green: core `fmt`/`clippy -D warnings`/`test --workspace --locked`; **`cargo build --target wasm32-unknown-unknown -p boundless-server-core` clean**; server crate unchanged (7 migration tests still pass); binding-drift lock regenerated (60 inputs); network allow-list clean (3 locks). **Worker shell deferred** (user decision "core engine only"): the `#[event]`/Router/`GroupHub` DO/Queues/KV/Turnstile + Postgres-over-Hyperdrive `AuthStore` (SET LOCAL RLS, non-superuser role, atomic `UPDATE…RETURNING`) + CSPRNG `SecretSource` + access-token signing + APNs/FCM + live atomic/TOCTOU integration tests → **`DEFERRED.md` → Server / core (T07) → T07-shell** (needs `docs-researcher` for workers-rs + the Postgres-over-Hyperdrive driver). I5 admin-PII-read audit + `audit_log` stay with spec 008 (T07 exposes **no** admin phone read). Reviewed via `reviewer` + `security-auditor` + `test-strategist` (findings addressed).
- **Done — Worker shell slice A (Postgres `AuthStore` adapter, native-tested), 2026-06-05:** Built **`boundless-server-store`** (`server/store/`, a NATIVE crate; `server/` is now a small workspace root + the `store` member) — `PgAuthStore`, the **`tokio-postgres`** SQL + transaction layer for nine `AuthStore` contract methods (member-by-phone lookup; onboarding load/`consume_if_live`; refresh `classify`/`rotate`/`revoke_family`/`create_family`; recovery load/`consume_and_rotate`), each in a transaction scoped by `set_config('app.current_group_id', $1, true)` (per-request RLS, fail-closed when unset). Methods are `async + Result` (mirror the sync `AuthStore` 1:1; the **async-port bridge** into `AuthService` is deferred — sync core can't block-on-async in wasm). **Driver decision → ADR-0019:** `sqlx` **cannot run in the Workers wasm runtime**, so the path is `tokio-postgres` over a Hyperdrive `Socket` (sqlx dropped from the stack). **13 real-`postgres:16` integration tests** (`server/store/tests/integration.rs`, self-skipping without `DATABASE_URL`): RLS isolate + unset-deny (fail-closed), single-consume **under real concurrency**, classify-superseded-after-N-rotations, **`concurrent_rotate_and_replay_resolves_to_revoked`**, family-kill-persisted-then-refused, recovery atomic + concurrency, member-roles round-trip, revoked-family-marks-superseded, onboarding consume-ignores-TTL, onboarding-superseded-not-live. The replay test **caught a real TOCTOU bug** (a concurrent rotate's new current row escaped a concurrent revoke under READ COMMITTED) — fixed with a `pg_advisory_xact_lock` on the family in both `rotate_session`/`revoke_family`. Pins (lock = ground truth): `tokio-postgres` 0.7.17 (`with-uuid-1`), `tokio` 1.52.3 (test-only) → `docs/stack-matrix.md` filled. Gates: server `fmt`/`clippy -D warnings`/`test --locked` (store self-skips without DB; root migration tests still pass); core `test --workspace --locked` **unchanged** (no core edits → binding-drift lock unchanged, 61 inputs); network allow-list clean (3 locks incl. the new `server/Cargo.lock`). CI: new **`server-store`** job (postgres:16 service → real integration run). **Out-of-scope register → `DEFERRED.md` → Server / store (slice A)**: the async-port bridge; the device-token store methods (`register_device` needs at-rest token encryption → spec 007/008); the wasm32 feature flags + pooler-safe `query_raw`; the Worker runtime itself → **T07-shell-B**. Reviewed via `reviewer` + `security-auditor`.
- **Done — Worker shell slice B / the async-port bridge (ADR-0020), 2026-06-05:** Wired the real `PgAuthStore` into `AuthService` by making `core/server`'s store ports **`async` + fallible**. Introduced a shared `StoreBackend { type Error }` supertrait so `AuthStore` (the 9 session/code/member methods) and a **new `DeviceStore`** port (the 3 device-token methods) share one error and the orchestration's `?` unifies; the endpoint methods (`sign_in`/`bind_device`/`refresh`/`recovery_rebind`) became `async … -> Result<_, St::Error>` (logic unchanged — only sync→async). **Why the split:** a literal `impl AuthStore for PgAuthStore` was blocked solely because `register_device` needs a *reversibly-encrypted* device token (push needs the plaintext back), and that at-rest encryption is deferred to spec 008 — so the device methods moved to `DeviceStore` and **`PgAuthStore` now `impl`s `AuthStore`** (the 9 ready methods). Reads take `&mut self` (Postgres txn needs it); trait futures are intentionally not `Send`-bound (`#[allow(async_fn_in_trait)]` — the wasm `?Send` Worker drives them; tests use a single-threaded executor); **no `async-trait` dep** (native AFIT). Adapted the **48 T07-core tests** to drive the async endpoints via host **`pollster::block_on`** (dev-only, zero-dep; a thin blocking extension trait keeps `#[test]` bodies readable). **New: 5 orchestration-over-Postgres tests** (`server/store/tests/service_pg.rs`) drive `AuthService` over the **real** `PgAuthStore` (composed with an in-memory `DeviceStore`) against `postgres:16` — sign-in match/miss (uniform shape), **atomic onboarding single-consume**, **refresh rotate-then-replay-kill** (family persisted `Revoked` in Postgres + AC15 alert), recovery consume-and-rotate, below-min degrades-without-consuming. Extracted the slice-A harness into `server/store/tests/common/mod.rs` (shared by both suites; also hardened `ensure_role` to swallow the concurrent-`CREATE ROLE` `unique_violation`, which the 13 parallel tests hit). Pin: `pollster` 0.4.0 (dev-only) → `docs/stack-matrix.md`. Gates green: core `fmt`/`clippy -D warnings`/`test --workspace --locked` (48 server-core green) + **`cargo build --target wasm32-unknown-unknown -p boundless-server-core` clean** (async trait stays wasm-safe); server `fmt`/`clippy`/`test --locked` with real DB (integration 13 + service_pg 5, stable over repeated runs; self-skip without `DATABASE_URL`); network allow-list clean (3 locks); binding-drift lock regenerated (61 inputs). **Out-of-scope register → `DEFERRED.md`** (async-port bridge ticked DONE; remaining T07-shell-B = the deployable workers-rs Worker runtime + the `PgDeviceStore` Postgres impl, which needs spec-008 token encryption). Reviewed via `reviewer` + `security-auditor`.
- **Done — Worker shell slice 2 / access-token decision + host-testable port impls (ADR-0021), 2026-06-05:** Resolved the plan §10-D **OPEN** access-token wire format and shipped the pieces it unblocks — all host/real-Postgres testable, **no `wrangler`/`worker-build` toolchain needed**. **(1) Decision → ADR-0021:** the access token is an **opaque-random 32-byte CSPRNG bearer** verified by a constant-time keyed-HMAC **store lookup** (the refresh idiom), **not EdDSA-JWT** — chosen via a read-only 4-reader/4-judge analysis (3–1, high confidence) because opaque honors the as-built **time-independent, family-status-gated** revocation model ("no stale token survives") **instantly**, fits the arg-less `SecretSource::fresh_access()` verbatim, and needs **zero new key-mgmt infra** (a JWT would need a deferred Ed25519 signing-key subsystem + a denylist to revoke + a port reshape). dryoc Ed25519 stays the **sole signature** impl (manifest only). **(2) W2 boot guard** (sec-audit's highest-impact item): `boundless_server_store::ensure_least_privilege(&client)` refuses if `current_user` is superuser or has `BYPASSRLS` (→ RLS bypass → cross-tenant PII); both legs proven vs real `postgres:16` (`server/store/tests/least_privilege.rs`). **(3) Access-token at-rest hash primitive:** `core::crypto::access_token_hash`/`access_token_matches` + `AccessTokenHash` (new domain tag `boundless:access-token:v1`; no `Debug`/`Display`/`Serialize`/`PartialEq`, compile-proved). **(4) Production `SecretSource`:** `RngSecretSource<R: RngCore + CryptoRng>` in `core/server` — opaque tokens from an **injected** CSPRNG (so the core stays randomness-free + `wasm32`-safe; the Worker injects a getrandom-backed RNG, tests a seeded `ChaCha20Rng`); a unit test cross-checks that a minted token verifies against the #3 hash. Pins (lock = ground truth): `rand_core` 0.9.5 (traits-only, `default-features = false` → no getrandom) prod + `rand_chacha` 0.9.0 dev — both **already in the lock**, so **no new crate versions** (the 0.9 `RngCore` API, not the 0.10 `Rng` rename, deliberately). Gates green: core `fmt`/`clippy -D warnings`/`test --workspace --locked` (crypto 19 incl. 2 new + secrets 6) + **`cargo build --target wasm32-unknown-unknown` clean** for `boundless-crypto` AND `boundless-server-core`; server `fmt`/`clippy`/`test --locked` with real DB (W2 1 + integration 13 + service_pg 5 + migrations 7); network allow-list clean (3 locks); binding-drift lock regenerated (63 inputs). **Out-of-scope → `DEFERRED.md`** (ADR-0021/W2-guard/hash-primitive/`RngSecretSource` ticked; remaining T07-shell-B = the Worker runtime + the access-token **store column + per-request verify lookup** (with the DO-fold/write-through-on-revoke guard-rail) + W2 call-site + `PgDeviceStore`; `fresh_recovery_code` human-format → confirm at spec-008). Reviewed via `reviewer` + `security-auditor`.
- **Done — T07-shell-B slice 1 (toolchain bring-up + deployable Worker skeleton), 2026-06-07:** Stood up
  the Worker toolchain (`worker-build` 0.8.3 via `cargo install`; the `wrangler` 4.98.0 +
  `@cloudflare/vitest-pool-workers` 0.16.13 + `vitest` 4.1.8 dev harness via pnpm — all reachable through
  the sandbox; **no Cloudflare account, no local Postgres needed**) and a **real, miniflare-tested Worker
  skeleton** in `server/` (`boundless-worker`): `#[event(fetch)]` + `worker::Router`, the `GroupHub`
  Durable Object, and the KV + Queues bindings, composing the **real core `AuthService::sign_in`** (P4)
  over a clearly-labelled **scaffold in-memory store** (`runtime/scaffold_store.rs` — deleted when
  `PgAuthStore` is wired). Routes: `GET /healthz` (version handshake AC7/O4 + a real KV `MANIFEST` read);
  `POST /api/auth/signin` (the frozen `api/openapi.yaml` `SignInResponse` wire shape —
  matched/not-on-file/below-min, no existence leak — + the below-min `AdminAlert` fanned out to the
  `ADMIN_ALERTS` Queue; the §10-E once-per-day dedup is **per-request** in the skeleton, routed through
  the DO in the PG slice — DEFERRED); `POST /api/auth/bind-device` (→ the `GroupHub` DO: the core §10-E
  rate-limit **window** over a synthetic `{member}` envelope + a durable counter via `state.storage()`,
  AC17 — **not** the full `BindDeviceRequest`). The crate is **cfg-split** (`worker`
  is a `cfg(target_arch="wasm32")` dep; the runtime is wasm-only) so the native `store`+`compat` legs stay
  green, and `default-members = ["."]` so `worker-build` never drags the native `store` crate onto wasm.
  **Gates green:** `worker-build --release` → 24 kB wasm; **6 miniflare tests** (vitest-pool-workers, no
  account); server `cargo fmt`/`clippy --workspace`/`clippy --target wasm32 -p boundless-worker`/`test
  --workspace` all `--locked`; core `test --workspace` unchanged (42 suites); **network allow-list clean
  across 6 locks** (the new `server/pnpm-lock.yaml` — wrangler/workerd/vitest/esbuild/sharp, no trackers);
  binding-drift unchanged (74 inputs — `server/` is not a drift input). Pins → `docs/stack-matrix.md`
  (Edge/Server). CI: new **`worker`** job (Ubuntu; GitHub-only). **Surfaced a frozen-contract defect**
  (OpenAPI `SignInRequest.phone_lookup_hash` is impossible under I3's per-instance server secret — the wire
  must carry the phone; needs an ADR) → `DEFERRED.md`. **Remaining T07-shell-B** (PgAuthStore-over-Hyperdrive
  + the forked-`tokio-postgres` ADR, access-token verify column, W2 call site, `PgDeviceStore`/APNs, Turnstile/429,
  `emit()` sink, manifest serving, `wrangler deploy` + live E2E) → `DEFERRED.md`. Reviewed via `reviewer` +
  `security-auditor` + `platform-parity`.

### T08 — Server: developer admin-create + Email Workers invite + compat harness — 🟡 CORE + STORE legs DONE (2026-06-05); deployable Worker endpoint + dev-WebAuthn + Email Workers → **T08-shell** (`DEFERRED.md`)
- **Does:** `/api/dev/admins` — developer-only, **hardware-key (WebAuthn) gated** (I11); mints a pending Admin + **single-use, 72h, server-time TTL** invitation token (AC16) delivered via **Email Workers** (ADR-0015 constraints: opaque token, no PII/credential in body). Stand up `server/tests/compat/` N-2 replay harness (O1).
- **Touches:** `server/` (`/api/dev/*`), Email Workers binding, `server/tests/compat/`.
- **Closes:** **AC1(a)** (dev endpoint authz: unauth + admin-auth both rejected), **AC9** (compat), **AC16** (mint/TTL leg).
- **Tests:** `ac1_admin_creation_rejects_unauth_and_admin`; `ac9_auth_endpoints_nminus2`; `ac16` mint side (single-use + server-time TTL); assert invite email body carries no PII/credential.
- **Blocked by:** T06 (admin tables), T03 (token hashing) · **∥:** with T07.
- **Done — core + store legs, 2026-06-05** (same functional-core/imperative-shell split as T07; **no `wrangler`/`worker-build`/Email-Workers toolchain here**): **(1) Authz decision (AC1(a) core leg):** `authorize_developer(DevCaller) → DeveloperAuthority | DevAdminCreateForbidden` in `core/server/src/admin.rs` — unauth **and** admin-auth both rejected (`DEV_ADMIN_CREATE_FORBIDDEN`, existing registry code); `DeveloperAuthority` is an **un-forgeable capability** (private field, only `authorize_developer` mints it) that `create_admin`/`reissue_admin_invite` take **by type**, so minting without dev auth does not compile (enforced-through-code, P-style). **(2) Mint orchestration (AC16):** `AuthService::create_admin` mints a 256-bit opaque invite (injected CSPRNG via `SecretSource::fresh_admin_invitation`), hashes it at rest (new `core::crypto::admin_invitation_token_hash`/`_matches` + `AdminInvitationTokenHash`, domain tag `boundless:admin-invitation:v1`, constant-time, no `Debug`/`Display`/`Serialize`/`PartialEq`), and returns the plaintext **once** for out-of-band delivery; server-time **72h** TTL (`INVITE_TTL_SECS`, injected `Clock`); `reissue_admin_invite` supersedes the prior live invite (lost-key recovery, ADR-0015). New tainted domain type `AdminInvitationToken`; the `AdminInvitation` mint output is **PII-free by construction** (opaque id + tainted token + expiry → compile-time `assert_not_impl_any!` Debug/Display/Serialize). **(3) Postgres store leg:** `impl AdminProvisioningStore for PgAuthStore` (`server/store/`) — `create_pending_admin_with_invitation` (one txn: INSERT pending Admin member role=`admin`, **no phone**, DB-minted id + INSERT invitation) and `reissue_admin_invitation` (**atomic supersede-then-insert** under an admin-scoped `pg_advisory_xact_lock`, so concurrent re-issues serialize and the `admin_invitations_one_live_per_admin` partial-unique index is never violated — closes the deferred "T08 admin invite re-issue"). Invitations store only a hash + opaque ids → **no encryption needed**, ships now. **(4) Compat harness (AC9):** `server/tests/compat/replay.rs` (`[[test]] compat`) replays the frozen `fixtures/compat/{current,n_minus_1,n_minus_2}.json` request shapes through `evaluate_version` against `minimum_supported(current, 2)` — accept current + N-2, degrade below floor (O1/O4/O8); plus an end-to-end `core/server/tests/compat.rs` through the real `sign_in` orchestration. **Tests:** core `admin.rs` ×5 (`ac1_admin_creation_rejects_unauth_and_admin`, `ac16_create_admin_mints_single_use_server_ttl_invitation`, `i11_admin_invite_token_single_use`, re-issue-unknown no-op, two-creates-independent) + `compat.rs` ×1 + secrets/no_formatter additions; crypto `invariants.rs` +2 (round-trip + domain-separation); real-`postgres:16` `server/store/tests/admin_invitations.rs` ×6 (create, supersede-keeps-one-live, unknown no-op, **role gate**, **tenant isolation (RLS)**, **concurrent re-issue keeps one live**). **No new external crate versions** (composes `hmac`/`sha2` + existing pins; the server compat harness adds `serde_json`/`boundless-auth`/`boundless-domain` **dev-deps** at the already-pinned versions). Gates green: core `fmt`/`clippy -D warnings`/`test --workspace --locked` (crypto 21, admin 5, compat 1, secrets 7) + **`cargo build --target wasm32-unknown-unknown -p boundless-crypto -p boundless-server-core` clean**; server `fmt`/`clippy`/`test --locked` with real DB (admin_invitations 6 + integration 13 + service_pg 5 + least_privilege 1 + compat 1 + migrations 7); network allow-list clean (3 locks); binding-drift lock regenerated (66 inputs). **Out-of-scope → `DEFERRED.md` → Server / admin-provisioning (T08)**: the deployable `#[event]` `/api/dev/admins` route + **Developer hardware-key WebAuthn verification** (constructs the `DeveloperAuthority`) + the HTTP-level AC1(a) integration test + **Email Workers** delivery + the email-body-no-PII wire assertion + the invite **consume** leg (T09) + `created_by` dev-id audit linkage. Reviewed via `reviewer` + `security-auditor`.

---

## Wave 5 — admin WebAuthn (edge)

### T09 — Admin WebAuthn verification on the SvelteKit edge (`@simplewebauthn/server`) — 🟡 VERIFICATION-CORE DONE (2026-06-05; Playwright-virtual-authenticator verified); deployable SvelteKit routes + real KV/Postgres bindings → **T15 / T09-shell** (`DEFERRED.md`)
- **Does:** Server-side WebAuthn **registration + assertion verification** in SvelteKit server routes (TS, `@simplewebauthn/server`), per ADR-0017: `userVerification: required`, `attestation: none`, discoverable creds, **multiple credentials per admin**; consumes the T08 invite token on first successful registration; **Developer re-invite revokes prior credential(s)** (ADR-0015/0016 D4). **KV challenge store, 5-min TTL, one-time-use** (ADR-0017 D3). Documented P4 carve-out (ADR-0017 D4).
- **Touches:** `web/src/lib/server/webauthn/`, `web/` server routes, KV binding, `admin_webauthn_credentials` (read/write).
- **Closes:** **AC16** (consume leg), **AC20** (UV/no-attestation/multi-cred/recovery); supports **AC2**.
- **Tests:** `ac20_webauthn_requires_uv_no_attestation_multi_credential` (Playwright virtual authenticator); `ac16_invite_expired_routes_and_ttl_server_side` (reuse→`InviteExpired`, server-time TTL); KV challenge one-time-use test.
- **Blocked by:** T08 · **∥:** no (gates T15).
- **Done — verification core + tests, 2026-06-05** (same functional-core/imperative-shell split as T07/T08; **no `wrangler`/SvelteKit runtime needed**): a **framework-agnostic WebAuthn verification module** `web/src/lib/server/webauthn/` (strict TS, runs in Node *and* the Workers runtime) behind **ports** — `Clock`, `ChallengeStore` (KV consume-once), `InviteStore`, `CredentialStore` — with in-memory fakes. **(1) Registration** (`register.ts`): `buildRegistrationOptions` bakes the ADR-0016 D4 policy (`attestation: none`, `residentKey: preferred`, `userVerification: required`, EdDSA/ES256/RS256, `excludeCredentials` = the admin's active creds), persists the challenge (300s); `verifyRegistration` consumes the challenge (one-time), enforces UV ourselves (verify with `requireUserVerification:false` then reject `uv=0` → distinct `ADMIN_WEBAUTHN_UV_REQUIRED`, R11), **consumes the invite** (single-use + server-time TTL → `ADMIN_INVITE_EXPIRED`/`ADMIN_INVITE_CONSUMED`, AC16), **revokes prior credentials** (D4 recovery), inserts the new credential (multi-per-admin, AC20). **(2) Assertion** (`authenticate.ts`): `buildAuthenticationOptions` (UV required) + `verifyAuthentication` (challenge consume → credential lookup → real verify → UV enforce → sign-count bump); assertion-only sign-in, AC2. **(3) Invite lifecycle** (`invite.ts` `evaluateInvite`): pure, server-time TTL. **(4) Error codes** (`errors.ts`): the 5 already-registered `ADMIN_*`/`ADMIN_WEBAUTHN_*` codes (no new codes) + `routesTo`. **Tests: 35 Vitest** (`ac16_invite_expired_routes_and_ttl_server_side`, `challenge_one_time_use`, options policy, multi-cred + revoke recovery, `webauthn_error_codes_match_registry`, the user-handle = opaque-adminId binding, revoked-credential rejection + challenge-consume-through-verify, and a `vi.mock`-stubbed verifier suite covering the R11 uv=0 reject + verified-false + verifier-throws + AC16 consume-on-success loop + D4 revoke + cloned-authenticator counter-regression — the review's defense-in-depth gaps) **+ 4 Playwright** (`ac20_webauthn_requires_uv_no_attestation_multi_credential`) driving Chromium's **CDP virtual authenticator** on a secure-context `http://localhost` page → **genuine attestation/assertion bytes through the real `@simplewebauthn/server` verifier**: UV registration verifies+stores+consumes-invite, valid UV assertion signs-in+bumps-counter (AC2), a **real `uv=0` assertion is rejected** (R11), and a **Developer re-invite registration revokes the prior credential** (D4). Toolchain pinned in `web/` (lock = ground truth): `typescript` 6.0.3, `vitest` 4.1.8, `@playwright/test` 1.60.0, `@types/node` 25.9.1 → `docs/stack-matrix.md` filled; `tsconfig` (strict), `vitest.config.ts`, `playwright.config.ts` (chromium-only). Gates green: `pnpm typecheck` (strict) + `pnpm test` (23) + `pnpm test:e2e` (4); **network allow-list clean** across the expanded `web/pnpm-lock.yaml` (3 locks, no trackers — the DEFERRED-flagged AC13 web re-scan); binding-drift **unchanged** (66 inputs; no `core/`/`api/` edits); core/server Rust suites untouched. New CI job **`web`** (typecheck → vitest → `playwright install chromium` → e2e; GitHub-only, not locally triggerable). **No user-visible strings** (the module emits error *codes*; the UI maps codes→catalog copy at T15) → `i18n-validator` N/A; the module performs **no logging** (returns/throws — P2). Reviewed via `reviewer` + `security-auditor`. **Out-of-scope → `DEFERRED.md` → Server / admin-WebAuthn (T09)**: the deployable SvelteKit `+server.ts` routes; the real Cloudflare **KV** `ChallengeStore`; the Postgres `admin_webauthn_credentials`/`admin_invitations` reads/writes via the Worker (incl. the **invite-token HMAC compare** routed through core per ADR-0017's P4 carve-out, the T08-flagged item); the post-assertion **httpOnly/Secure/SameSite session cookie** (§10-F); the **additive backup-key enrollment** flow (the authenticated, no-revoke add-key path — AC20's "encouraged backup key" second half; the invite-gated path here is correctly revoke-and-replace recovery per D4) + its `ac20_register_passkey_and_backup_key` test (plan §7), which need the admin session → **T15**; **AC11b** axe-core + keyboard ceremony (UI = T15); the live deployed-edge E2E; the `webauthn-rs`-sidecar fallback (ADR-0017).

---

## Wave 6 — contracts (FREEZE GATE)

### T10 — API contracts + generated bindings (contract-freeze) — 🟡 CONTRACTS FROZEN + AC7 DONE (2026-06-05); **T10-shell Swift leg (BoundlessKit UniFFI XCFramework) DONE (2026-06-05)**; **contract amendment ADR-0023 (auth requests carry `phone`, not a client hash) DONE (2026-06-07)**; remaining codegen (OpenAPI-Swift client, Kotlin/TS, UniFFI AAR) → **T10-shell** (`DEFERRED.md`)
- **Does:** Finalize `api/openapi.yaml` (`client_min_version` **required** on every `/api/auth/*` response) + `api/boundless.proto` (WS open-handshake carries `client_min_version`). Generate `api/generated/<lang>/` (swift-openapi-generator / openapi-generator kotlin / openapi-typescript + ts-proto); build the UniFFI **XCFramework** + **AAR**. **Freeze** before any UI starts.
- **Touches:** `api/openapi.yaml`, `api/boundless.proto`, `api/generated/**`, `apple/BoundlessKit/`, `android/core-bridge/`, `web/src/lib/api/generated/`.
- **Closes:** **AC7** (contract leg — OpenAPI + proto required-field tests).
- **Tests:** `ac7_auth_responses_require_min_and_recommended_version` (OpenAPI); `ac7_ws_handshake_has_client_min_version` (proto); generated-binding-drift CI gate green.
- **Blocked by:** T05, T07 (shapes stable), T08/T09 (admin shapes) · **∥:** no (blocks Wave 7).
- **Done — contract-freeze + AC7, 2026-06-05** (same functional-core/imperative-shell split as T07/T08/T09; the codegen toolchains — `buf`/`protoc`/`openapi-generator`/`swift-openapi-generator`/`uniffi-bindgen`/`wasm-pack` — are **not installed**, and the consuming UIs T11–T15 are themselves toolchain-blocked, so real binding generation is deferred, not faked): **(1) `api/openapi.yaml` FROZEN** (OpenAPI 3.1, `info.version 1.0.0`) — the four `/api/auth/*` endpoints (signin / bind-device / refresh / recovery·rebind) plus `/api/dev/admins` + `/api/admin/auth/{invite,register,signin}`. Response shapes **mirror the frozen golden fixtures** `fixtures/auth/*.json` (the authoritative HTTP **wire** shapes — the core's internal response structs are the orchestration form the deferred Worker shell maps to the wire, P4), reusing the verified serde sub-objects (`AppVersion`→string, `Platform`/`Role` enums, `ManifestPointer`). The AC7 anchor is a shared **`VersionHandshake`** (`required: [client_min_version, client_recommended_version]`) that every auth response **`allOf`**s, so both fields are required in every `oneOf` routing variant. Domain outcomes ride in the **200 body** (no 4xx for not-on-file/below-min — HTTP status would be an existence oracle; carry-forward (b)). Secret-bearing fields (`access_token`/`refresh`/`fresh_recovery_code`) are documented **optional** sensitive fields (TLS-to-legit-client, ADR-0021/0016) so the contract is complete without putting secrets in the committed fixtures. Every `error_code` already exists in `docs/error-codes.md` (no new codes). **(2) `api/boundless.proto` FROZEN** (proto3, handshake-only) — `ClientVersion` / `ClientOpenHandshake` / **`ServerOpenHandshake { client_min_version = 1; client_recommended_version = 2; }`** (the WS mirror of the HTTP version handshake, AC7/O4/O5); matching/realtime msgs are later specs. **(3) AC7 tests:** `ac7_auth_responses_require_min_and_recommended_version` — **`web/` Vitest** (`web/tests/contract/api-contract.test.ts`, parses the YAML with the new `yaml` 2.9.0 devDep; a `$ref`+`allOf` flattener asserts the version-field union on every `/api/auth/*` response + non-vacuity + the VersionHandshake anchor); `ac7_ws_handshake_has_client_min_version` — **`core/sync` Rust, dependency-free** (`core/sync/tests/proto_contract.rs`, `include_str!` + single-pass comment-stripper + brace-matched message scan; 4 tests incl. scanner/stripper regression guards — the stripper one caught & fixed a real bug where `/api/auth/*` inside a `//` line tripped block-comment detection). Pinned `yaml` 2.9.0 (ISC, built-in types, ESM — verified via docs-researcher; `docs/stack-matrix.md` filled + API-contracts freeze note); `web/vitest.config.ts` include extended to `tests/contract/**`. Gates green: web `pnpm typecheck` + `pnpm test` (37 = 35 webauthn + 2 contract) + `pnpm test:e2e` (4); core `fmt`/`clippy -D warnings`/`test --workspace --locked` (incl. 4 proto-contract tests); **binding-drift regenerated + in sync (67 inputs)** + meta-test green; network allow-list clean (3 locks incl. the `yaml`-expanded `web/pnpm-lock.yaml` — the DEFERRED AC13 web re-scan). `generate-bindings.sh` stays scaffold mode; `api/generated/**` stays `.gitkeep` placeholders (wording in the script + `api/generated/README.md` updated: contracts frozen in T10, codegen → T10-shell). No user-visible strings (codes map to catalog at the UI) → `i18n-validator` N/A. **Reviewed via `platform-parity` + `reviewer` + `security-auditor`: 0 critical/high merge-blockers, all privacy invariants clean.** Findings addressed: parity's one real **P4 drift** — the frozen wire/fixture's `ManifestPointer.locale_key_prefix` (mandated by ADR-0014/spec §C.6) was missing from the core `ManifestPointer` — **fixed at the source** by completing the core struct (`core/server/src/service.rs` + 2 test call-sites), so the wire now faithfully mirrors the core (P4); the OpenAPI header reworded to honestly describe the wire as a flattened/renamed **projection** of the core orchestration structs (not verbatim "reuse"); the OpenAPI AC7 test hardened to pin coverage to the contract's own `/api/auth/*` path set (not a magic count); the proto `declares_field` made depth-aware (a field nested in a child message can't satisfy the assertion) + regression guards; security's invite-token-in-URL **shell obligation** (log-scrub + `Referrer-Policy: no-referrer`) recorded in the T09 register. **Out-of-scope → `DEFERRED.md` → API contracts / codegen (T10)**: the real per-target codegen (swift-openapi-generator + protoc-gen-swift → `api/generated/swift/` + `BoundlessKit` XCFramework; openapi-generator-kotlin + protoc-gen-kotlin → `api/generated/kotlin/` + `core-bridge` AAR; openapi-typescript + ts-proto → `api/generated/typescript/` + `web/src/lib/api/generated/`), wired in `generate-bindings.sh` per target **with T11–T15** when the toolchains exist; T02's platform-parity UniFFI mapping notes; the live deployed-edge contract-conformance E2E (with the Worker shell, T07-shell-B).
- **Done — contract amendment ADR-0023 (auth-request phone-on-wire), 2026-06-07** (resolves the
  `DEFERRED.md` "OpenAPI auth-request ↔ I3 contract defect" surfaced at the T07-shell-B Worker wiring):
  the frozen `SignInRequest`/`BindDeviceRequest`/`RecoveryRebindRequest` claimed a **client-computed**
  `phone_lookup_hash`, but I3 keys that hash with a **per-instance server secret** a client cannot hold,
  and `core::server::sign_in` already takes the raw **phone** and hashes server-side — so the contract was
  internally inconsistent with I3 as built. **Option A** (per a user decision this session; **ADR-0023**):
  amend the three auth requests to carry the E.164 **`phone`** (TLS-protected; the server normalizes +
  HMAC-hashes + drops the plaintext — never logged P2 / only the keyed hash + `phone_encrypted` stored,
  I3/I5); rejected Option B (client keyless pre-hash — unsalted low-entropy hash is brute-forceable +
  violates P4 single-source `normalize_phone`). Changed: the 3 OpenAPI request schemas + signin path/field
  descriptions; the 3 illustrative `fixtures/compat/**` bodies; spec 001 §C step 2 + AC3 prose (AC3
  checkbox unchanged — bookkeeping convention); the drift lock. **New regression test**
  `web/tests/contract/api-contract.test.ts` — "all three auth requests require `phone` and expose no
  `phone_lookup_hash`" (the AC7-adjacent guard). **No Rust source change** (the engine was already
  correct; only a stale comment in `server/tests/compat/replay.rs`). Gates green: web `pnpm typecheck` 0 +
  `pnpm test` **70** (+1); core + server compat/fixtures cargo tests; binding-drift in sync (74 inputs);
  allow-list clean (6 locks). Reviewed via `reviewer` + `security-auditor` + `platform-parity`
  (`i18n-validator` N/A — no catalog copy). **Unblocks** the T10-shell OpenAPI request codegen and the
  T07-shell-B PgAuthStore sign-in lookup, which now consume a consistent contract.
- **Done — T10-shell, Swift leg = BoundlessKit (UniFFI XCFramework), 2026-06-05** (prerequisite that FFI-unblocks T11; the "toolchain not installed" reading was wrong for Apple — Xcode 26.5 is installed, just not `xcode-select`'d, so the build uses `DEVELOPER_DIR`): **(1)** `core/ffi-swift` now carries the UniFFI surface — `uniffi` 0.31.1; mirror enums (`OnboardingState`/`OnboardingEvent`/`LaunchDecision`/`SignInResult`/`BindResult`/`Role`) + **exhaustive `From` conversions** (compile-checked parity guard) + `#[uniffi::export]` free fns (`launch`/`on_event`/`is_terminal`/`allows_offline_overlay`/`reauth_state_for`/`should_flag_notifications_off`); `crate-type=["lib","staticlib","cdylib"]`; the `uniffi-bindgen` CLI is a `[[bin]]` behind a **host-only** `bindgen` feature (no clap in the iOS `.a`). The wasm core stays uniffi-free (**ADR-0022**); only the client-relevant state machine crosses — no `AppVersion`/`MemberId`/tainted/PII type, no secret (P2). **(2)** `scripts/build-boundlesskit.sh` (cargo iOS device+sim `.a` + host cdylib → `uniffi-bindgen --library` → `module.modulemap` rename → `xcodebuild -create-xcframework`) + `scripts/test-boundlesskit.sh` (auto-detects an iPhone sim) → `apple/BoundlessKit/` SwiftPM package. **(3) Tests:** 5 host round-trip/transition tests in `core/ffi-swift` + a **5-assert XCTest smoke test that passes on the iPhone 17 simulator** (`** TEST SUCCEEDED **` — Rust→UniFFI→Swift on-device). Gates green: core `fmt`/`clippy -D warnings`/`test --workspace --locked` (incl. the 5 ffi-swift tests); `cargo build --target wasm32 -p {domain,crypto,auth,server-core}` still clean (ffi-swift deliberately not wasm-built, never on the wasm path); binding-drift regenerated + in sync (**68 inputs**) + meta-test; network allow-list clean (3 locks incl. the uniffi-expanded `core/Cargo.lock`). New CI job `boundlesskit` (`macos-15`; GitHub-only, not locally verifiable). The XCFramework + generated Swift are **git-ignored build artifacts** (reproducible from `core/ffi-swift`, which the drift gate tracks via `core/**`) — distinct from the committed `api/generated/**`. ADR-0022 + `docs/stack-matrix.md` (`uniffi` 0.31.1; Apple `BoundlessKit` row) updated. **Reviewed via `platform-parity` + `reviewer` + `security-auditor`.** **Out-of-scope → `DEFERRED.md`**: the OpenAPI Swift HTTP client (→ T11), `core/ffi-kotlin` AAR (→ T13/T14), proto-Swift (→ realtime), committing+drift-gating the artifacts (optional). **T11 itself (the Rider SwiftUI screens) is NOT started** — next slice, now FFI-unblocked.

---

## Wave 7 — per-platform UI (all parallel)

### T11 — SwiftUI Rider onboarding UI — ✅ UI LEGS DONE (2026-06-05); networking/Keychain/APNs/manifest + `.xcodeproj` app shell → **T11-shell** (`DEFERRED.md`)
- **Does:** All Rider onboarding screens per the state machine (helper intro → phone entry → Onboarding Code → permissions(+declined) → auto-update step/enabled → silent complete); the calm `BelowMinVersion`/`NeedsReauthHelp` screens (never a form); `Offline` overlay. Consume `core::auth` via `BoundlessKit`. Rider Settings exposes **no** auto-update toggle.
- **Touches:** `apple/BoundlessRider/`, `RiderShared`.
- **Closes:** **AC5, AC6, AC8(snapshot), AC11, AC14(UI), AC15(Rider snapshot), AC1(b)** (no-signup-route, iOS).
- **Tests:** `swift-snapshot-testing` ×4 variants per screen; `AutoUpdateStep` resolves `onboarding.autoupdate.enabled`; `NeedsReauthHelp` no-form snapshot; `RiderSettings` no-toggle; VoiceOver traversal; `ios_onboarding_no_signup_route`.
- **Blocked by:** T10 · **∥:** T12–T15.
- **Done — UI legs, 2026-06-05** (same functional-core/imperative-shell split as T07–T10: the deployable shell — OpenAPI Swift HTTP client, Keychain refresh-store §10-F, APNs, signed-manifest fetch/verify, and the shippable `.xcodeproj` app bundle — is deferred because the snapshot/UI/inspection legs `tasks.md` assigns T11 need none of it, and the Worker those calls target doesn't exist yet, T07-shell-B): **(1)** new SwiftPM package `apple/BoundlessRider` (`RiderShared` library + tests, no `.xcodeproj`; `defaultLocalization "en"`, iOS 17; local-path dep on `BoundlessKit`). The screens **render** the `core::auth` state machine via `BoundlessKit` (`onEvent`/`launch`/`reauthStateFor`/`allowsOfflineOverlay`/`shouldFlagNotificationsOff`) and never decide transitions (P4): `OnboardingScreenModel` (single source for both rendering AND a11y reading order — no drift) + `OnboardingScreenView` (built-in semantic colors + button styles → dark-mode-safe; `.font` text styles → Dynamic Type; `ScrollView` reflow), `RiderOnboardingScreens` factories, `@Observable @MainActor OnboardingViewModel` (side effects — network/notification/manifest — behind injected `@MainActor` protocols), `OnboardingRouter`, `RiderSettings` (AC6: no auto-update toggle — **structural**, the enum has no such case). **(2)** String Catalog `Onboarding.xcstrings` (hand-authored JSON, `extractionState:"manual"`, translator comments, `%1$@` positional with the admin name repeated in `auth.below_min_version`) — the spec's 14 keys **plus** 11 flagged keys (catalog total 25): the affordance/settings set the screens require (`onboarding.action.{continue,try_again}`, `onboarding.permissions.{allow,decline}`, `settings.{title,notifications,help}` — the spec enumerates body copy, the a11y design mandates a control per step) and 4 name-less `*_generic` fallbacks (review-driven, mirroring `auth.below_min_version_generic`, so the name-bearing screens never render an empty `%1$@` slot when no manifest is cached) — resolved via `L10n` from `Bundle.module` (P8: no string literals in views). **(3) Tests (44, green on the iPhone 17 Pro sim):** 17 snapshot tests × 4 a11y variants = **68 committed baselines** (default / largest Dynamic Type / dark / RTL, `perceptualPrecision 0.98`, pinned `iPhone13` config) + 27 logic tests: `AutoUpdateStep` resolves `onboarding.autoupdate.enabled` (AC5), `RiderSettings` no-toggle (AC6), `BelowMinVersion` no-CTA + name-repeat (AC8), `NeedsReauthHelp` no-form (AC15), `ios_onboarding_no_signup_route` (AC1(b)), VoiceOver reading-order incl. "auto-update enabled" announced as a state not a button (AC11), Permissions-decline advances + flags via the core (AC14), + the full view-model traversal. Gates: `scripts/test-boundlessrider.sh`; new `boundlessrider` `macos-15` CI job (GitHub-only); network allow-list extended to scan `Package.resolved` (swift-snapshot-testing 1.19.2 + graph — clean, I8); `docs/stack-matrix.md` filled. Reviewed via `reviewer` + `platform-parity` + `security-auditor` + `i18n-validator`. **Out-of-scope → `DEFERRED.md` → Apple / T11-shell.**

### T12 — SwiftUI Driver onboarding UI — ✅ UI LEGS DONE (2026-06-05); networking/`RecoveryCodeProviding`/Keychain/APNs/manifest + `.xcodeproj` app shell + self-serve re-bind ENTRY UI → **T12-shell** (`DEFERRED.md`)
- **Does:** Driver onboarding (self-run); **Recovery Code one-time capture** screen (D3); Driver session-expiry routes to interactive re-auth (`auth.signin_again`).
- **Touches:** `apple/BoundlessDriver/`.
- **Closes:** **AC11, AC14(UI), AC15(Driver branch), AC1(b), AC19(UI)**.
- **Tests:** snapshots ×4; Recovery-Code-capture snapshot; Driver re-auth route test; `ios_driver_no_signup_route`.
- **Blocked by:** T10 · **∥:** T11,T13–T15.
- **Done — UI legs, 2026-06-05** (same functional-core/imperative-shell split as T07–T11; the deployable shell — OpenAPI Swift client incl. `/api/auth/recovery/rebind`, the real `RecoveryCodeProviding`, Keychain §10-F, APNs, manifest, the `.xcodeproj` app bundle `app.boundless.driver`, and the self-serve re-bind ENTRY UI — is deferred because the snapshot/UI legs T12 closes need none of it, and the Worker those calls target doesn't exist yet, T07-shell-B): **(1)** new SwiftPM package `apple/BoundlessDriver` (`DriverShared` library + tests, no `.xcodeproj`; `defaultLocalization "en"`, iOS 17; local-path deps on `BoundlessKit` **and** `BoundlessRider`). The Driver flow **renders** the `core::auth` state machine via `BoundlessKit` and never decides transitions (P4); `DriverOnboardingViewModel` **composes** `RiderShared.OnboardingViewModel(role: .driver)`, so the Driver/Rider divergence (`reauth_state_for(.driver) == .phoneEntry` vs `.rider == .needsReauthHelp`) is the core's decision. **Reuses the `RiderShared` kit** (screen model + single `OnboardingScreenView` renderer + `L10n` + the injected protocols + the role-neutral screen factories) — no duplication; adds only the 3 Driver deltas: `driverIntro` (self-onboard `FreshInstall`), `reAuthPhoneEntry` (leads with `auth.signin_again`, AC15 Driver branch — a Driver self-re-auths, so it *is* a form, unlike the Rider's form-less `NeedsReauthHelp`), and `recoveryCodeCapture` (the one-time AC19 capture, shown after bind via a router view-ack interstitial on `.permissions`). **(2)** Driver String Catalog `DriverOnboarding.xcstrings` (4 new keys, flagged for owner review) + `DriverL10n`; shared copy resolves through `RiderShared.L10n`. **Two minimal, non-breaking extensions to the shared `RiderShared` kit:** the `L10n.signInAgain` accessor (the seam T11 deliberately left) and a new `BodyElement.code` element (prominent monospaced/selectable, for the Recovery Code) rendered in the shared `OnboardingScreenView` — additive cases, so **T11's 68 baselines re-verified green** (`test-boundlessrider.sh` → `** TEST SUCCEEDED **`). **(3) Tests (21, green on the iPhone 17 Pro sim):** 18 Driver screens × 4 a11y variants + the silent-completion placeholder ×4 = **76 committed baselines** (default / largest Dynamic Type / dark / RTL, `perceptualPrecision 0.98`, pinned `iPhone13`) + logic: `RecoveryCodeCaptureTests` (capture content + code-in-reading-order + available-after-bind + nil-code-skips + Driver-only, AC19), `DriverReauthRouteTests` (core Driver-vs-Rider routing + VM `sessionInvalidated → phoneEntry+reauthRequested` + re-auth screen is a signInAgain-led form, AC15), **`test_ios_driver_no_signup_route`** (AC1(b)), `DriverOnboardingViewModelTests` (happy path + AC14 decline-and-flag + below-min override + resume), `VoiceOverTraversalTests` (AC11 reading order incl. the code as static text), `NameLessFallbackTests`. Gates: `scripts/test-boundlessdriver.sh`; new `boundlessdriver` `macos-15` CI job (GitHub-only); `.gitignore` adds `apple/**/.swiftpm/`; network allow-list scans the new `Package.resolved` (swift-snapshot-testing 1.19.2 graph — clean, I8); `docs/stack-matrix.md` noted (no new dependency). Reviewed via `reviewer` + `platform-parity` + `security-auditor` + `i18n-validator`. **Out-of-scope → `DEFERRED.md` → Apple / Driver UI (T12).**

### T13 — Compose Rider onboarding UI — ✅ UI LEGS DONE (2026-06-06); MainActivity/networking/Keystore/manifest/notifications + Kotlin OpenAPI codegen → **T13-shell** (`DEFERRED.md`)
- **Does:** Android Compose mirror of T11 (TalkBack semantics, 48dp targets).
- **Touches:** `android/rider/app/` (the module **now exists** — a configured Compose+Paparazzi stub with a green sample snapshot; T13 replaces the sample with the real screens, rendered from `:core-bridge`).
- **Closes:** **AC5, AC6, AC8(snapshot), AC11, AC14(UI), AC15(Rider), AC1(b)** (Compose).
- **Tests:** `Paparazzi` ×4 variants; TalkBack traversal; no-toggle / no-signup-route.
- **Blocked by:** T10 (+ Android bring-up — **DONE**: `core/ffi-kotlin` UniFFI AAR + `android/` Gradle project + SDK/NDK/cargo-ndk, all proven green; see `DEFERRED.md` → "Android bring-up"). · **∥:** T11,T12,T14,T15.
- **Done — UI legs, 2026-06-06** (same functional-core/imperative-shell split as T07–T12; the deployable shell — the launcher `MainActivity` composition root, the OpenAPI Kotlin HTTP client, `NotificationManager` permission + FCM, signed-manifest fetch/verify, and EncryptedSharedPreferences/Keystore refresh storage — is deferred because the snapshot/UI/logic legs T13 closes need none of it, and the Worker those calls target doesn't exist yet, T07-shell-B): the faithful Android twin of `RiderShared` in `android/rider/app` (package `app.boundless.rider`). The Compose screens **render** the `core::auth` state machine via the `:core-bridge` UniFFI AAR and never decide transitions (P4): a pure `OnboardingScreenModel` (the SAME value renders AND derives the a11y order — no drift) + `BodyElement`/`ScreenAction`/`FieldModel`/`A11yDescriptor`; one `OnboardingScreenView` renderer (Material3 semantic colors → dark-safe, type styles → font-scale, `verticalScroll` reflow, `semantics{heading()}`, the confirmation as static-text-not-button, ≥48dp targets); `RiderOnboardingScreens` factories; an `OnboardingViewModel` (plain class, `mutableStateOf`, the single `applyEvent` = `onEvent` via the FFI; suspend boundaries injected; **no Hilt** — deps via ctor, mirroring iOS); `OnboardingRouter` (state→screen, view-local acks; silent `Complete`); `RiderSettings` (no auto-update toggle — structural, AC6). **i18n (P8):** `res/values/strings.xml` = the catalog (the iOS 25 keys, dot→underscore, escaped, positional `%1$s`); a `RiderStrings` interface (the `L10n` twin, generic-fallback selection centralized); the production `AndroidRiderStrings` (R.string) is the deferred shell, while tests + snapshots use `CatalogRiderStrings` — which **parses the real strings.xml** (single source of truth, zero deps, no Robolectric/emulator) so the Paparazzi snapshots render genuine copy without relying on Paparazzi resource resolution. **Tests (11 classes, green on host JVM + layoutlib):** `OnboardingSnapshotTest` = 17 screens × 4 a11y variants = **68 committed Paparazzi baselines** (default / largest `fontScale=2f` / dark `NightMode.NIGHT` / RTL `LayoutDirection.RTL`, `supportsRtl=true`); plus `OnboardingViewModelTest` (P4 happy path + branches via `runTest`), `PermissionsDeclineTest` (AC14), `TalkBackTraversalTest` (AC11 model-derived reading order), `AutoUpdateTest` (AC5), `RiderSettingsTest` (AC6), `BelowMinVersionTest` (AC8), `NeedsReauthHelpTest` (AC15), `NoSignupRouteTest` (AC1(b)), `NameLessFallbackTest`, `CatalogRiderStringsTest`. New dep: `kotlinx-coroutines` 1.9.0 (+ `-test`; Kotlin-2.0-compatible) → `docs/stack-matrix.md` filled; **no Hilt/Turbine/Robolectric**. Removed the bring-up `BringUpSample` + its baseline. Verified: `scripts/build-corebridge.sh` → `:rider:app:{recordPaparazziDebug,verifyPaparazziDebug}` green, `./gradlew test` (debug+release) green, both apps `assembleDebug`; `cargo build --target wasm32` core still clean; binding-drift unchanged (69 inputs — `android/` is not a drift input); network allow-list clean (5 locks) + the Android tracker gate clean (coroutines = JetBrains). **Out-of-scope → `DEFERRED.md` → Android / Rider UI (T13-shell).** The TalkBack walkthrough is the manual a11y leg (plan §7). T14 (Compose Driver) is the sibling slice, now also pattern-unblocked.

### T14 — Compose Driver onboarding UI — ✅ UI LEGS DONE (2026-06-06); launcher MainActivity/real RecoveryCodeProviding+networking/Keystore/manifest/FCM + the recovery-rebind ENTRY UI → **T14-shell** (`DEFERRED.md`)
- **Does:** Android Compose mirror of T12 (incl. Recovery Code capture, driver re-auth).
- **Touches:** `android/driver/app/` + the new **`:rider:shared`** com.android.library (the extracted role-neutral kit both apps depend on — the Android twin of iOS's `RiderShared`; an app module can't depend on another app module).
- **Closes:** **AC11, AC14(UI), AC15(Driver), AC1(b), AC19(UI)** (Compose).
- **Tests:** `Paparazzi` ×4; Recovery-Code snapshot; re-auth route; no-signup-route.
- **Blocked by:** T10 (+ Android bring-up — **DONE**, see T13). · **∥:** T11–T13,T15.
- **Done — UI legs, 2026-06-06** (same functional-core/imperative-shell split as T07–T13; the deployable shell — the launcher `MainActivity` composition root, the real `RecoveryCodeProviding`/`OnboardingNetworking` (OpenAPI Kotlin client), `NotificationManager`+FCM, signed-manifest fetch/verify, Keystore refresh storage, and the self-serve recovery-rebind ENTRY UI — is deferred because the snapshot/UI/logic legs T14 closes need none of it, and the Worker those calls target doesn't exist yet, T07-shell-B): the faithful Android twin of `DriverShared` in `android/driver/app` (package `app.boundless.driver`). **First**, the role-neutral kit was extracted **behavior-preservingly** from `:rider:app` into a new `com.android.library` **`:rider:shared`** (`git mv` with Kotlin packages unchanged — only the module R-namespace differs — so the rider imports + 68 baselines are untouched; both apps now depend on it). **Then** the Driver adds only the three T12 deltas, all rendering `core::auth` via `:core-bridge` and never deciding transitions (P4): `DriverOnboardingScreens` (`driverIntro` / `reAuthPhoneEntry` (the AC15 interactive re-auth form, leading with `auth.signin_again` — the contrast to the Rider's form-less calm screen) / `recoveryCodeCapture` (AC19 — the one-time Recovery Code via the shared `BodyElement.Code` element, ADR-0016 D3)); `DriverOnboardingViewModel` (composes the shared `OnboardingViewModel(role = DRIVER)` + the injected `RecoveryCodeProviding`; adds `reauthRequested`, set **before** delegating `sessionInvalidated()` to the core); `DriverOnboardingRouter` (state→screen, the PERMISSIONS recovery-capture interstitial + the PHONE_ENTRY re-auth branch; `when`-exhaustive; silent `Complete`); `DriverTheme`. **i18n (P8):** a Driver `strings.xml` with the 4 Driver keys (verbatim from `DriverOnboarding.xcstrings`) merged with the shared `:rider:shared` catalog; `DriverStrings : RiderStrings` (the `DriverL10n`+`L10n` twin). **Tests (8 classes, 44 tests, green host JVM + layoutlib):** `DriverOnboardingSnapshotTest` = 19 screens × 4 a11y variants = **76 committed Paparazzi baselines** (default / `fontScale=2f` / `NightMode.NIGHT` / `LayoutDirection.RTL`, `supportsRtl=true`); plus `DriverOnboardingViewModelTest` (P4 + AC14), `DriverReauthRouteTest` (AC15 — `reauthStateFor(DRIVER)==PHONE_ENTRY` via the FFI), `RecoveryCodeCaptureTest` (AC19), `TalkBackTraversalTest` (AC11 model-derived order incl. the code as static text), `NoDriverSignupRouteTest` (AC1(b)), `NameLessFallbackTest`, `CatalogDriverStringsTest` (the merged shared+Driver resolver). The catalog test resolver is single-sourced in `:rider:shared/main` + the tiny fakes are per-app (AGP `testFixtures` can't host Kotlin on the pinned KGP 2.0.21 — Kotlin 2.1.0 feature; pre-approved fallback). **No new dependency versions** (coroutines already pinned at T13). Verified: `scripts/build-corebridge.sh` → `:driver:app:{recordPaparazziDebug,verifyPaparazziDebug}` green + `:rider:app`'s 68 baselines + logic tests re-verified **unchanged**; `./gradlew test` (debug+release) green, both apps `assembleDebug`, `:rider:shared:assembleDebug`; `cargo build --target wasm32` core still clean; binding-drift unchanged (69 inputs — `android/` is not a drift input); network allow-list clean (5 locks) + the Android tracker gate clean. **Reviewed via a 4-lens adversarial workflow** (`reviewer` + `platform-parity` + `i18n-validator` + `security-auditor`): **0 merge-blockers** — platform-parity/i18n PASS clean, security-auditor found no live P2/PII violation (the Recovery Code is render-yes/log-never; zero logging in the slice). Acted on: removed the empty source folders the kit move left (reviewer nit). The forward-looking hardening notes (a tainted `RecoveryCode` newtype + `FLAG_SECURE` on the capture window + `allowBackup="false"` before the shell persists credentials) are folded into **`DEFERRED.md` → Android / Driver UI (T14-shell)**. The recorded TalkBack walkthrough is the manual a11y leg (plan §7).

### T15 — SvelteKit admin onboarding UI — ✅ UI LEGS DONE (2026-06-05); ✅ T15-shell **leg A** (real Cloudflare KV challenge store + adapter-cloudflare) DONE (2026-06-07); ✅ leg-B item (i) **fail-closed challenge-store selector** DONE (2026-06-07); Postgres invite/credential bindings + persistent session store + `wrangler deploy` → **T15-shell leg B** (`DEFERRED.md`)
- **Does:** Invite-link landing → WebAuthn **registration ceremony** → `InviteExpired` → WebAuthn **sign-in** (no password field). Consumes T09 verification + the generated TS client.
- **Touches:** `web/` routes + components.
- **Closes:** **AC2, AC11b, AC1(b)** (web no-signup-route).
- **Tests:** Playwright + axe-core zero-violations on all 4 routes; `ac2_no_password_field`; keyboard-only WebAuthn ceremony; 200%/400% reflow; `aria-live` on expired/error; RTL/dark.
- **Blocked by:** T09, T10 · **∥:** T11–T14.
- **Done — UI legs, 2026-06-05** (same functional-core/imperative-shell split as T07–T12; the deployable shell — real Cloudflare **KV** challenge store + **Postgres** invite/credential stores via the Worker, the persistent server-side session store, and the `@sveltejs/adapter-cloudflare` + `wrangler` deploy — is deferred because the UI/a11y/ceremony legs T15 closes need none of it, and `wrangler` is not installed, T07-shell-B): **(1)** scaffolded the **SvelteKit 2 + Svelte 5** app in `web/` (`svelte.config.js`, `vite.config.ts` with `@tailwindcss/vite`, `src/app.{html,css,d.ts}`, `+layout.{svelte,server.ts}`, `hooks.server.ts`); `pnpm typecheck` = `svelte-kit sync && svelte-check` (strict, 0/0). The four onboarding surfaces **render the T09 verification core's outcomes** and never re-implement WebAuthn (P4 carve-out per ADR-0017 is the *only* non-core auth logic): `admin/onboard/[token]` (SSR-resolved invite → register-ceremony prompt **or** `InviteExpired`, no loading flash) + `admin/signin` (assertion only, **no password field** — AC2) + `admin/` (signed-in placeholder; dashboard = spec 008), with `+server.ts` endpoints mirroring the frozen contract (`GET invite/[token]`, `POST register`, `GET/POST signin`) over the **T09 in-memory port fakes** (the real KV/Postgres adapters drop into `webauthn-deps.ts` unchanged — shell). **(2)** Browser ceremony via **`@simplewebauthn/browser` 13.3.0** (statically imported — SSR-safe; keeps `create()`/`get()` inside the user-activation window). **(3)** i18n via **`intl-messageformat` 11.2.8** (`src/lib/i18n`: catalog + cached `t(key,locale,values)` + `Intl.Locale().getTextInfo()` direction; **no string literals in views** — P8); 13 keys (the spec's 2 admin keys + 11 affordance/status/success keys, flagged for owner review). Error-code→catalog-key map kept **client-safe** (string keys, never imports `$lib/server`) with a node parity test vs the T09 `WEBAUTHN_ERROR_CODES` + `error-codes.md`. **(4)** §10-F **session cookie** (httpOnly + Secure + SameSite=Strict) minted post-assertion (in-memory session store; persistent store = shell). **(5) Tailwind v4** styling (semantic HTML, `focus-visible` rings, `dark:`, logical-property RTL, ≥44px targets); `<html lang dir>` set per-request in `hooks.server.ts`. **Tests:** **54 vitest** (T09 23 + T10 contract 2 + new i18n/session/errors) + **11 Playwright e2e** (4 T09 + **7 T15**): `ac11b_axe_zero_violations` (×{default,dark,RTL} on signin/onboard-live/InviteExpired), `ac2_no_password_field`, `web_onboarding_no_signup_route` (AC1(b)), real **register→sign-in** happy path (CDP **virtual authenticator** → real bytes → real `@simplewebauthn/server`; invite single-use re-checked AC16; session-cookie flags), `ac11b_webauthn_keyboard_only`, reflow 200%/400%, aria-live on InviteExpired + binding error. A `data-hydrated` layout marker + a `gotoReady` helper make the e2e hydration-deterministic. Pins (lock = ground truth) → `docs/stack-matrix.md`: `@sveltejs/kit` 2.63.0, `svelte` 5.56.1, `vite` 8.0.16, `@sveltejs/vite-plugin-svelte` 7.1.2, `@sveltejs/adapter-node` 5.5.4, `svelte-check` 4.6.0, `tailwindcss`+`@tailwindcss/vite` 4.3.0, `@simplewebauthn/browser` 13.3.0, `@axe-core/playwright` 4.11.3 (+ `axe-core` 4.11.4), `intl-messageformat` 11.2.8. Gates: `pnpm typecheck`/`test`/`build`/`test:e2e` green; **network allow-list clean across 5 locks** (the DEFERRED AC13 web re-scan — closed); **binding-drift unchanged (68 inputs)** (`web/` not a drift input); core/server Rust untouched. CI `web` job extended (`pnpm build` + the T15 e2e; GitHub-only). `melt-ui`/Radix **not** pulled (semantic HTML clears AC11b; → spec 008). **Reviewed via a 4-lens adversarial workflow** (`reviewer` + `i18n-validator` + `security-auditor` + `platform-parity`, each finding independently verified): 3 confirmed, all **fixed** — the admin auth routes now emit the **frozen-contract status codes** (`GET invite` 200/**410**, `register`/`signin` 200/**400**, the opaque token leaks no existence on a 410) **and the frozen response body schemas** (`WebAuthnRegistrationOptions{publicKey,challenge_ttl_secs}` / `InviteExpired{error_code,routes_to}` / `AdminAuthResult{admin_id}` / `ErrorBody{error_code}`) so they genuinely mirror `api/openapi.yaml`; the SSR load now resolves invite **status only** (`resolveInviteStatus`, no wasted ceremony/challenge/cookie). **Out-of-scope → `DEFERRED.md` → Admin web / SvelteKit onboarding (T15)** + **Android toolchain + Compose UI (T13/T14 — BLOCKED)**.
- **Done — T15-shell leg A (real Cloudflare KV challenge store), 2026-06-07** (account-free; the recommended next leg per the T15-shell investigation): the admin-WebAuthn one-time-use challenge store (ADR-0017 D3) is now the **real Cloudflare KV** store, not the in-memory stub. **(1)** `KvChallengeStore` (`web/src/lib/server/webauthn/kv-challenge-store.ts`) implements the existing `ChallengeStore` port over a `KVNamespace` — `put` with `expirationTtl` (60s-floor clamp helper `kvExpirationTtl`), `take` = `get('text')` then `delete` (best-effort consume-once; the file documents the KV no-atomic-get+delete reality + the defence-in-depth: one-time httpOnly+Secure+SameSite=strict ceremony cookie, `@simplewebauthn` challenge binding, sign-count check). **(2)** Proven against **real Miniflare KV** with **no Cloudflare account** via wrangler's `getPlatformProxy()` (`kv-challenge-store.test.ts`: round-trip / consume-once / absent→null / key-isolation + the clamp unit). **(3)** Made **live**: swapped `@sveltejs/adapter-node` → `@sveltejs/adapter-cloudflare` (during `vite dev` the adapter injects `platform.env` via `getPlatformProxy`, so the binding is real locally — verified from the adapter source); `webauthn-deps.ts` selects `KvChallengeStore` when `platform.env.CHALLENGES` is bound, else the in-memory fallback (Vitest/adapterless); the three `+server.ts` routes thread `event.platform`; `App.Platform` typed in `app.d.ts`; `web/wrangler.toml` adds the `CHALLENGES` binding (+ a commented Hyperdrive stub for leg B). The Playwright register→sign-in ceremony now round-trips the challenge through real Miniflare KV. Pins (lock = ground truth) → `docs/stack-matrix.md`: `wrangler` 4.98.0, `@sveltejs/adapter-cloudflare` 7.2.8, `@cloudflare/workers-types` 4.20260606.1 (`@sveltejs/adapter-node` removed). Gates green: `pnpm typecheck` (0/0) · `pnpm test` (66, incl. 6 real-KV) · `pnpm build` (adapter-cloudflare, no account) · `pnpm test:e2e` (13) · **network allow-list clean across 6 locks** (the expanded `web/pnpm-lock.yaml` + `web/pnpm-workspace.yaml` allow-builds for workerd/esbuild/sharp); binding-drift unaffected (`web/` not a drift input); Rust untouched. **Reviewed via `reviewer` + `security-auditor`.** **Out-of-scope → `DEFERRED.md` → T15-shell leg B**: making the challenge-store selector **fail-closed** when `CHALLENGES` is unbound in a production edge build (the in-memory fallback is dev/test-only — the top review finding, latent today as there's no deploy target); generating `App.Platform` via `wrangler types` once bindings multiply; the Postgres invite/credential stores over Hyperdrive (+ the invite-token HMAC via the core, ADR-0017 P4) — couples to the unbuilt Worker runtime (T07-shell-B) + account; the persistent server-side session store behind the §10-F cookie; the dev-only `/api/test/*` seam removal; the invite-token URL log-scrub + `Referrer-Policy: no-referrer`; `wrangler deploy` + the live deployed-edge E2E; the authenticated additive backup-key enrollment (needs the persistent session).
- **Done — T15-shell leg-B item (i): fail-closed challenge-store selector, 2026-06-07** (account-free; the top finding from leg A's own review, pulled forward because it's the one leg-B item that needs no Cloudflare account / Postgres / Worker runtime): the challenge-store selection is now the **pure** `selectChallengeStore(kv, fallback, allowInMemoryFallback)` in `web/src/lib/server/webauthn/kv-challenge-store.ts` (type-only imports → Vitest-testable; `webauthn-deps.ts` can't be unit-tested as it pulls SvelteKit virtuals). Real KV when the `CHALLENGES` binding is present; the per-isolate in-memory fallback **only** when `dev` (`$app/environment`, statically inlined to `false` in any `vite build` — confirmed in the built `chunks/webauthn-deps.js` call site `selectChallengeStore(platform?.env?.CHALLENGES, challenges, false)`); else it **throws** a PII-free operator 500 naming ADR-0017 D3. This closes the multi-isolate consume-once hole (a per-isolate `Map` can't guarantee one-time-use across edge isolates) — a binding misconfiguration now hard-fails instead of silently degrading. `webauthn-deps.ts:challengeStore` delegates, passing `dev`. **Tests** (`kv-challenge-store.test.ts`): throws prod-no-kv / falls back dev-no-kv (pure) + picks the real KV when bound (real Miniflare KV — no fake-KV cast). Gates: `pnpm typecheck` 0/0 · `pnpm test` **69** (+3) · `pnpm build` (adapter-cloudflare, no account) · `pnpm test:e2e` **13** (the dev ceremony still round-trips the KV branch — no dev behaviour change) · network allow-list clean (6 locks); no dep change; Rust untouched. **Reviewed via `reviewer` + `security-auditor`: ship (0 crit/high/med).** Two forward-looking notes carried to `DEFERRED.md` → T15-shell leg B item (i): **(F1)** when the web scrubbed `emit()` sink + a `handleError` hook land, route this boundary's uncaught throw through `emit()` + add an I10 scrubber fixture for the operator string (PII-free today, so no leak); **(F2, optional)** a build-artifact test asserting `dev`→`false` at the call site (the automated form of the manual grep). Remaining leg B (Postgres stores / deploy / session store / generated types / invite-URL hardening) unchanged.

---

## Wave 8 — cross-cutting verification

### T16 — Cross-cutting verification sweep — ✅ DONE (2026-06-06)
- **Does:** Run the suite-wide checks across all surfaces: log-scrubber replay over onboarding fixtures incl. error/offline branches (P2/I10); network allow-list against all lock files (I8); pseudo-locale (`zz-ZZ`) render across every onboarding screen; compat replay (O1).
- **Touches:** `fixtures/onboarding/log_lines.jsonl`, CI checks, all UI test suites.
- **Closes:** **AC3** (scrubber leg), **AC12** (pseudo-locale), **AC13** (allow-list), **AC9** (final compat run).
- **Tests:** scrubber replay (zero PII reaches scrubber); `ac13_onboarding_adds_no_third_party`; `pseudo_locale_renders_all_onboarding_screens`.
- **Blocked by:** T07–T15 · **∥:** no (final gate).
- **Done — 2026-06-06** (same functional-core/imperative-shell split as T07–T15; the deployable I10 logging *pipeline* — `boundless::logging::emit()` sink + no-raw-`tracing` lint + Logpush/latest-run CI replay — and the native `zz-ZZ` pixel-truncation snapshots stay deferred to **T07-shell-B** / each platform's -shell, because they need the unbuilt Worker runtime / per-platform render harness, while every invariant T16 gates needs neither): **(1) AC3 scrubber leg** — new dep-free, wasm32-safe crate **`core/logging` (`boundless-logging`)**: `detect_pii()` is a hand-rolled byte scanner (no `regex`) for email / phone (10–15 digit runs) / token-or-secret blob (≥40 contiguous hex/base64url) / street / GPS, with **canonical-UUID suppression** so the opaque non-PII `MemberId`/`SessionFamilyId` ids (and version strings) are never flagged — without which a UUID's all-numeric 12-hex tail would be misread as a phone and over-redact (I10's "fail on any redaction"). The new **`fixtures/onboarding/log_lines.jsonl`** (23 representative PII-free events covering `PhoneNotOnFile`/`BindingFailed`/rate-limit/below-min/offline/manifest-fallback/refresh-replay + all 4 `AdminAlert`s, every `error_code` from `docs/error-codes.md`) is replayed (`onboarding_replay.rs`) asserting **zero PII on every line**; the **red-team self-test** (`scrub_redteam.rs`) proves the detector has teeth (catches each PII shape) *and* the false-positive guard (UUIDs/versions/error-codes/event-names/ISO-timestamps never flagged) — so "clean" is meaningful, not vacuous. **12 tests** (2 unit + 1 replay + 9 red-team). **(2) AC12 pseudo-locale** — new `web/src/lib/i18n/pseudo.ts` (`pseudoize()` = accent + ~40% pad + `⟦…⟧` brackets, **ICU `{arg}`/`<tag>`-preserving**) wired as `catalogs['zz-ZZ']` (opt-in via `?locale=zz-ZZ`; default `en` unaffected). A **cross-platform parity** Vitest gate (`tests/cross-platform/catalog-parity.test.ts`) reads **all five** shipped catalogs (iOS `.xcstrings` JSON, Android `strings.xml`, web TS) and asserts iOS-Rider⟺Android-Rider (24 keys) + iOS-Driver⟺Android-Driver (4) key-set identity (dot↔underscore-normalized — catches the drift no per-platform test can see) + that every key on every surface has a valid padded/bracketed/ICU-preserving pseudo expansion (`pseudo_locale_renders_all_onboarding_screens`, data leg). A **Playwright** spec (`tests/e2e/pseudo-locale.spec.ts`) renders the real admin onboarding screens in `zz-ZZ` and asserts the visible copy is bracketed (every string resolved through the catalog — P8) + **no horizontal overflow at the 200%/400% reflow widths** (the "without truncation" leg, rendered). **(3) AC13** — `tests/cross-platform/network-allowlist.test.ts` (`ac13_onboarding_adds_no_third_party`) invokes `scripts/check-network-allowlist.sh` across all 5 locks (I8). **(4) AC9** — verified the existing T08 compat replay (`server/tests/compat/replay.rs::ac9_auth_endpoints_nminus2` + `core/server/tests/compat.rs`) green as the final run; no new code. **No new dependencies** (Rust scrubber = std-only; web = already-pinned Vitest/Playwright; pseudoize = pure TS). Gates green: core `fmt`/`clippy -D warnings`/`test --workspace --locked` (incl. the 12 logging tests) + **`cargo build --target wasm32-unknown-unknown -p boundless-logging` clean**; web `pnpm typecheck` (strict, 0/0) + `pnpm test` (Vitest incl. 5 cross-platform) + `pnpm test:e2e` (Playwright incl. 2 zz-ZZ); `scripts/check-network-allowlist.sh` clean (5 locks); binding-drift lock regenerated (`core/logging` is a new `core/**` input). CI: `rust-core` `--workspace` picks up `boundless-logging`; `web` job runs the new Vitest + Playwright (step names updated; GitHub-only). **Reviewed via a 3-lens adversarial workflow** (`reviewer` + `security-auditor` + `i18n-validator`): the central question — is the AC3 scrubber a REAL gate or vacuous? — came back **REAL** (detector has teeth + a false-positive guard; fixture honestly PII-free; UUID-suppression correctly scoped; allow-list + pseudoize verified). Findings actioned: (a) the **dotted-phone** false-negative (most common US format) — **fixed** (`.` added to phone bytes + an IPv4 structural exclusion + red-team cases); (b) **single / separate-JSON-field GPS** (`"lat":…,"lng":…`) — **fixed** (single high-precision-component detector + red-team cases); (c) the **vacuous ICU-preservation assertion** (no shipped catalog has an `{arg}` yet) — **fixed** with a synthetic nested-plural `pseudoize` round-trip through `IntlMessageFormat`; (d) over-claiming doc language softened. Remaining **latent** detector gaps (sub-40-char tokens, lowercase-only addresses, bare-dash digit-glue — none in the onboarding flow) folded into the DEFERRED scrubber-hardening item for the T07-shell-B `emit()` sink. **Out-of-scope register → `DEFERRED.md` → Cross-cutting verification (T16)**: `emit()` sink + no-raw-`tracing` lint + Logpush replay → T07-shell-B; native `zz-ZZ` pixel-truncation snapshots → per-platform -shell; scrubber hardening → pre-GA. **Every acceptance criterion's planned functional-core / testable leg is now green** (see the AC coverage check below — all ✓); the deployable shell legs (T07-shell-B + per-platform -shells) remain tracked in `DEFERRED.md`, so spec 001 is **complete to the functional-core/imperative-shell waterline, not yet deployably complete** (spec.md's AC boxes are reserved for end-to-end sign-off).

---

## Post-T16 — CI-hardening follow-ups (from `DEFERRED.md`, not numbered tasks)

> Account-free, locally-verifiable invariant gates picked up after the T01–T16 waterline, one slice each.

- **FFI mirror-crate surface-parity gate — ✅ DONE (2026-06-07).** `core/ffi-swift/tests/parity_with_kotlin.rs` asserts the two UniFFI mirror crates' (`core/ffi-swift` ⇄ `core/ffi-kotlin`) `lib.rs` exported surfaces stay **byte-identical** (the production region before `#[cfg(test)]`, comments/blanks stripped), catching an **FFI-only divergence** (P4/P7) that the compile-time `From`-`match` guard cannot see. Rides the `rust-core` CI job; no new wiring. Reviewed (`platform-parity` + `reviewer`: ship, 0 crit/high/med; F3 hardening = `#[cfg(test)]` must appear exactly once). See `DEFERRED.md` → Android bring-up register.
- **"No ambient randomness in core" wasm gate (sec-audit F6) — ✅ DONE (2026-06-08).** `scripts/check-wasm-no-getrandom.sh` (+ meta-test `scripts/test-wasm-no-getrandom.sh`) builds `boundless-server-core` + `boundless-crypto` for `wasm32-unknown-unknown` and asserts no `getrandom 0.3.x` (the OS-randomness backend) on each crate's **production** (`-e no-dev`) wasm tree, with a positive control that only the allowed `0.4.x` (`wasm_js`-shim) edge is present (ADR-0021). Version-agnostic forward-tree scan (reviewer + security-auditor caught a version-pinned `-i getrandom@0.3.4` false-pass on a `cargo update` patch bump). Step in the `rust-core` CI job; no new wiring/deps. Coverage of `ffi-wasm`/`sync`/`logging`/`server/` deferred (dependency-free today) → `DEFERRED.md` Crypto register. See `DEFERRED.md` → Crypto register (F6).
- **Reproducible proptest seeds gate (P9 — DEFERRED T05 register) — ✅ DONE (2026-06-08).** Makes P9 "seeds checked into the repo" mechanical: committed `core/{auth,server}/proptest-regressions/.gitkeep` + `scripts/check-proptest-regressions.sh` (+ meta-test `scripts/test-proptest-regressions.sh`). The gate **auto-discovers** proptest crates from `core/**/Cargo.toml` (section-aware awk — inline `[…dependencies]`/dotted `[…dependencies.proptest]`, excluding the `[workspace.dependencies]` registry) and per crate asserts the `proptest-regressions/` dir is git-tracked AND a future seed is not gitignored; fail-closed + zero-crates non-vacuity guard. Step in the `rust-core` CI job; no new deps. Two transient negatives bit (untracked → FAIL; gitignored → FAIL); binding-drift unchanged at 75 inputs. Reviewer "fix H1 [stage scripts] then ship" (done); security-auditor "ship it" (the `.gitkeep`-tracked-but-`*.txt`-ignored trap is closed). Discovery hardening (dotted-section + commented-header false-pass vectors) added in-slice from the two reviews. See `DEFERRED.md` → `core::auth` (T05) register.
- **T07-shell-B fail-closed scaffold-deploy guard (security-auditor F1) — ✅ DONE (2026-06-08).** Makes it impossible to *silently* deploy the Worker's in-memory scaffold (hardcoded `SCAFFOLD_HMAC_KEY = [0x7b; 32]` + seeded demo member). The scaffold is gated behind a **non-default `scaffold` cargo feature**: `server/src/lib.rs` gates `mod runtime` on `feature="scaffold"` and `compile_error!`s a featureless wasm build, so the **test** path opts in (`package.json` `build` + the CI `worker` job's `pnpm test`) while the **deploy** path (`wrangler.toml [build]`, featureless) **fails closed**. Enforced by `scripts/check-worker-deploy-guard.sh` (featureless wasm build must fail *via* the compile_error sentinel — non-vacuity covers "guard missing" + "broke for another reason"), a step in the CI `worker` job. Folds in the **F3 nit**: `DEMO_PHONE` → `+15555550100` (NANP reserved `555-01XX`). Locally proven (pnpm test green, featureless fails, gate green, two transient non-vacuity checks bit, native+drift+allow-list green). Reviewer + security-auditor both "ship it" (0 crit/high/med). The **real close** (delete the scaffold + retire the feature/guard) rides the future PgAuthStore slice. See `DEFERRED.md` → "Server / core (T07)" register.
- **Worker→Postgres driver-choice ADR (T07-shell-B unblock) — ✅ DONE (2026-06-08).** Account-free decision doc (no code) that `DEFERRED.md` mandated **before** any PgAuthStore-over-Hyperdrive build. **ADR-0024:** stay on the **published `tokio-postgres` 0.7.17** and issue queries via the **unnamed-statement typed family** (`query_typed_one`/`query_typed_opt`/`execute_typed` (writes)/`query_typed[_raw]`; `simple_query`/`batch_execute` for no-param/DDL) — **no driver fork** (the blog-cited `unnamed-statement` fork is dead/404, a supply-chain risk on the auth path; the published 0.7.17 already exposes the API, whose own doc names "Cloudflare Workers with Hyperdrive", verified in the vendored `client.rs`). **Refines + corrects ADR-0019** (its "pooler dislikes *unnamed*" wording was inverted — *named/persistent* statements are what break; *unnamed* is the fix). Bookkeeping threaded through `DEFERRED.md` (T07-shell-B + store register), `docs/stack-matrix.md`, ADR-0019 ("Refined by" pointer). No dependency change; no binding-drift inputs touched. The build slice it unblocks (migrate the 9 store methods to the typed family — `query_typed_one`/`query_typed_opt`/`execute_typed` + wasm flags + Socket transport + real-pooler/deployed E2E) still needs a Cloudflare account + a local Postgres. See `docs/adr/0024-hyperdrive-unnamed-statements.md`.

---

## Deferred (NOT tasks here — tracked in `DEFERRED.md`)

- The two remaining new privacy-invariant tests land with the **deletion** work, not this spec: extend the I12 forgetting property test to the auth artifacts, and the named delete-leg device-token invalidation (the refresh-rotation-replay test **is** in T05). Account deletion flow itself is out of scope (spec §Out of scope).
- Critical Alerts capability upgrade (post-entitlement); the `@simplewebauthn`→`webauthn-rs`-sidecar fallback if edge support breaks.

---

## AC coverage check

AC1✓(T08+T11–T15) AC2✓(T15/T09) AC3✓(T03+T16) AC4✓(T05+T07) AC5✓(T11/T13) AC6✓(T11/T13) AC7✓(T07+T10) AC8✓(T07+T11/T13) AC9✓(T08+T16) AC10✓(T03) AC11✓(T11–T14) AC11b✓(T15) AC12✓(T16) AC13✓(T16) AC14✓(T07+T11–T14) AC15✓(T05/T07+T11–T14) AC16✓(T08+T09) AC17✓(T04+T07) AC18✓(T05+T07) AC19✓(T05/T07+T12/T14) AC20✓(T09/T15). **All 21 acceptance criteria covered.**
